# 第二问对标复盘（2026-09-23）

## 结论

截图中的方案展示了两个清楚的方向：A 侧压低架次和能耗，B 侧实现零软延迟且更早完工。我们原先虽然逐箱、逐机、逐电池均可复核，但调度搜索的时间顺序写错了，造成非必要的迟到；单服务区大批货箱又被锁在 C 型机上，拖长末段。这是求解质量问题，不能用“模型更严谨”掩盖。

## 已定位的问题与修正

1. 原 `_job_key` 给所有普通货箱的期望时间加了 `1,000,000`，使 10800 秒硬截止批次始终优先于 3600 秒期望批次。现改为在同一时间轴比较，并对每个候选计划重新执行硬截止检查。
2. 原邻域搜索仅拆分多服务区航线，无法拆分单服务区大批货箱。现增加 C 型机单点批次的完整二分枚举，重新计算两条航线并分配实体无人机、电池与开始时间。
3. 原输出基本只有一个主目标和两套对照，不足以说明时效与资源取舍。现导出零延迟较省能耗、交付时刻优先、限时节能、节能方案时序改良等方案，并分别核验。
4. 节能方案的时序改良会改变第三问通信缺口。完整流程继续用经第三问中继求解验证可接续的原“限时节能”方案作初值；改良版单独作为第二问对照，不混用两问结果。
5. 后续审查发现跨路线搬箱的“末尾插入”分支会在已修改列表上再次追加同一货箱，导致该候选因重复箱被拒绝。现已改为每个插入位置从原路线独立构造，并加入跨路线换箱、更多搜索轮次以及任务顺序—机型—实体机—电池联合束搜索。
6. 新增两阶段 ALNS：第一阶段保护零软延误和加权交付，第二阶段在加权交付不超过第一阶段 103% 的条件下，利用搬箱、换箱、合并、拆分、访问重排和移除重插压缩架次与能耗；算子概率每 50 次按历史收益更新。

## 数值变化

以下数据均由本仓库原始附件与当前统一物理模型复算，时间单位为秒，软延误为 `优先系数 × 正延误` 对无硬截止货箱求和。

| 方案 | 架次 | 能耗 kWh | 最晚返航 s | 加权软延误 |
| --- | ---: | ---: | ---: | ---: |
| 修正前主方案 | 24 | 75.804 | 10099.547 | 94935.285 |
| 当前主方案 | 28 | 77.059 | 7558.434 | 0 |
| 当前零延迟较省能耗方案 | 27 | 76.401 | 7531.737 | 0 |
| 当前 ALNS 零延迟节能方案 | 25 | 74.337 | 7531.737 | 0 |
| 当前限时节能方案 | 23 | 67.929 | 9345.191 | 72248.943 |
| 当前节能时序改良方案 | 23 | 67.768 | 9377.377 | 30223.122 |

截图报告 B 侧为 26 架次、76.34 kWh、7042 秒完工、软延迟 0。若其“完工”也指末架返航，且采用相同 DEM、能耗、期望时间、交接计时、电池充电口径，则当前 ALNS 方案少 1 架次、少约 2.00 kWh，但慢约 490 秒；它的加权交付时刻为 2,809,611.621，截图未给出同口径指标，无法比较总体交付先后。截图报告 A 侧等权方案为 20 架次、66.34 kWh、9166 秒完工；我们最省能耗的方案仍多 3 架次、多 1.43 kWh、慢约 211 秒。截图没有提供逐箱表、航线、DEM/能耗公式和资源时序，因此其软延误数值和可行性还不能按同一口径独立复核。此处只比较截图明确列出的汇总值，不推断对方错误。

## 仍需改进

- 当前已用有界束搜索联合选择固定路线集内的任务顺序、机型、实体无人机和电池，但它只保留有限宽度的部分计划，因此仍不是全局最优证明。后续可扩大状态宽度，或对候选路线集建立精确调度模型并报告求解界。
- Q1 的逐服务区精确组批只是 Q2 初值，不保证跨服务区和多架次总目标最优。当前 ALNS 已有搬箱、换箱、拆分、合并和 2—4 箱移除重插，但搜索预算有限，仍缺少更大规模的路线破坏重建以及完整帕累托候选库。
- 更优的第二问轨迹未必能被现有第三问中继方案覆盖。后续应把通信缺口反馈到运输邻域搜索，统一比较“运输加中继”可行方案。
- 对外比较必须交换同一套逐箱交付、航段时间与能耗、电池 SOC/充电、通信覆盖日志，再按统一脚本重新计算指标；仅凭截图无法判定哪套模型更正确。

本轮改动与运行结果见 `q2.py`、`q2_alns.py`、`Q2_交付说明.md`、`results_q2/q2_tradeoff.csv` 和 `results_q2/q2_validation.json`。
