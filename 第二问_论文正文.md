# 第二问 异构无人机多点多架次运输调度

## 1 问题分析

在第一问中，每架次只服务一个服务区，且不考虑实体无人机与共享电池的周转。第二问允许一架次依次访问多个服务区，因而可以利用投送后载荷逐段下降的特点减少重复往返；同时，一条路线能否执行，还取决于其开始时刻能否同时取得对应机型的实体无人机和充满电的电池。路线、资源和时间相互影响：合并架次可能降低能耗，却会推迟沿途部分货箱的交付；将同一批货拆成较多架次则可能改善时效，但增加起降准备、装箱和往返消耗。因此，将货箱组批、访问顺序与资源调度共同纳入求解。

本问不考虑通信保障。全部 80 个货箱从调度中心 O01 运往 15 个服务区，货箱不可拆分且每箱只交付一次。附件配置 A、B、C 三种运输机型，分别有 4、2、2 架实体机，以及 6、4、4 组同型共享电池。医疗物资必须在其期望时间之前送达，首批保障货箱必须满足首批截止时间；两项要求同时适用时，取更早者作为硬截止。其他货箱的期望时间用于计量延误。任务完成时刻为所有运输架次返抵 O01 的最晚时刻，而非最后一箱的交付时刻。

## 2 符号与评价指标

记货箱集合为 $I$，服务区集合为 $V$，机型集合为 $G=\{A,B,C\}$，已生成的运输架次集合为 $K$。货箱 $i$ 的目的地、质量、体积、优先系数、期望送达时刻分别记为 $v_i,w_i,c_i,p_i,d_i$；如有硬截止，记为 $h_i$。架次 $k$ 的机型、实体机、电池、起始时刻、返航时刻、运输能耗分别记为 $g_k,u_k,b_k,s_k,r_k,E_k$，货箱 $i$ 的实际交付时刻为 $t_i$。所有时刻均以任务开始为零点，单位为秒。

为避免题目没有指定四项指标权重而任意设定加权系数，主方案按下列字典序目标评价完整可行计划：

$$
\min\left(D,\;W,\;C_{\max},\;E_{\mathrm{tot}},\;N\right),
$$

其中，$I_{\mathrm{soft}}$ 为没有硬截止的货箱集合，

$$
D=\sum_{i\in I_{\mathrm{soft}}}p_i\max\{0,t_i-d_i\},\qquad
W=\sum_{i\in I}p_i t_i,
$$

$$
C_{\max}=\max_{k\in K}r_k,\qquad
E_{\mathrm{tot}}=\sum_{k\in K}E_k,\qquad N=|K|.
$$

$D$ 反映普通物资的加权软延误，$W$ 在 $D$ 相同时进一步区分整体送达先后；$C_{\max}$、$E_{\mathrm{tot}}$ 和 $N$ 依次反映最晚返航、运输能耗和架次数。硬截止不靠罚函数处理，而是在产生候选计划时逐箱检验 $t_i\leq h_i$。这一优先顺序是本研究采用的决策口径，并非题目给定的唯一偏好；因此另保留时效和资源侧重不同的对照方案。

## 3 运输与资源约束模型

### 3.1 航线时间和能耗

一个架次表示为 $O01\rightarrow v_{k1}\rightarrow\cdots\rightarrow v_{km}\rightarrow O01$。每个服务区交接结束时，才记该处货箱完成交付。架次出发前计入机型规定的准备时间和逐箱装载时间；访问每个服务区时计入固定交接时间及逐箱交接时间。相邻节点之间采用水平直线航段，计划巡航海拔取沿线 DEM 最高地形以上 50 m；O01 的作业高度为其地面海拔，服务区为地面海拔以上 30 m。每次服务区交接后，下一航段从该服务区的作业高度重新爬升。

对机型 $g$ 的某航段，设水平距离为 $\ell$，爬升、下降高度为 $H^+,H^-$，当前剩余载荷为 $q$，则航段时间按附件的爬升、巡航、下降速度计算：

$$
T_g=\frac{H^+}{v_g^+}+\frac{\ell}{v_g}+\frac{H^-}{v_g^-}.
$$

题目规定载荷相关等效航程及水平能耗与爬升附加能耗之和，但未给出两个能耗分量的唯一计算式。本文采用项目中的显式假设：

$$
L_g(q)=L_g^0-\bigl(L_g^0-L_g^F\bigr)
\left(\frac{q}{Q_g}\right)^{1.5},
$$

$$
E_g(\ell,H^+,q)=\frac{B_g\ell}{L_g(q)}
+\frac{(m_g^0+q)\,9.81\,H^+}{3.6\times10^6\eta_g}.
$$

式中，$L_g^0,L_g^F$ 为空载与满载等效航程，$Q_g$ 为最大载质量，$B_g$ 为单组电池可用能量，$m_g^0$ 为空机质量，$\eta_g$ 为爬升效率。下降不另计附加能耗。到达某服务区并完成交接后，将该区已交付货箱的质量从 $q$ 中扣除，再计算下一航段。该能耗分解是计算假设，结果的物理精度受其影响。

对架次 $k$，装载集合 $I_k$ 必须满足

$$
\sum_{i\in I_k}w_i\leq Q_{g_k},\qquad
\sum_{i\in I_k}c_i\leq V_{g_k},\qquad
E_k\leq (1-\rho_{g_k})B_{g_k}.
$$

同时要求每个货箱恰好属于一个架次，在目的服务区交付，且沿线 DEM 高程能够判定。三种机型的载质量上限依次为 25、30、80 kg，装载容积上限依次为 0.060、0.073、0.250 m³；本数据的返航安全余量均为 20%。

### 3.2 实体无人机与共享电池

对于同一实体无人机，其负责的架次占用区间 $[s_k,r_k]$ 不能重叠。电池按机型共享，不同机型之间不得混用。电池初始 SOC 为 100%；架次 $k$ 返航后的 SOC 为 $\sigma_k=1-E_k/B_{g_k}$，再次投入前须充至 100%。设机型对应电池的等效完全充电时间为 $F_g$，则两阶段充电时间为

$$
R_g(\sigma)=
\begin{cases}
F_g\left[0.65\dfrac{0.9-\sigma}{0.9}+0.35\right], & 0\leq\sigma<0.9,\\[6pt]
F_g\,0.35\dfrac{1-\sigma}{0.1}, & 0.9\leq\sigma\leq1.
\end{cases}
$$

若实体机 $u$ 与电池 $b$ 的下一次可用时刻分别为 $a_u,a_b$，则新架次最早可从 $s_k=\max\{a_u,a_b\}$ 开始；分配后更新 $a_u=r_k$、$a_b=r_k+R_{g_k}(\sigma_k)$。从而同一电池的飞行占用和充电区间不能与其后续任务重叠。不同电池可并行充电。运输机未另设题目未给出的架次周转时间，新架次自身的准备和装箱时间已计入占用区间。

## 4 求解方法

首先，依据第一问的单区可行组批生成初始架次，并按硬截止与普通期望时间处于同一时间轴的原则安排候选执行顺序。这样，较晚的硬截止批次不会仅因其约束类型而抢占较早期望时间货箱的机位。对每条候选路线枚举可行机型，逐段计算时间、载荷、能耗及各箱交付时刻；质量、体积、返航余量或硬截止不合格的路线与分配立即剔除。

随后，在路线层搜索架次合并、跨架次移箱与换箱、单架次拆分及服务区访问顺序调整。对固定的路线集合，采用有界束搜索联合选择任务顺序、机型、实体机和同型电池；每个部分状态记录资源下一次可用时刻及已产生的目标值，仅保留有限个较优状态。这一步能缓解“当前最早发出的架次”占用稀缺电池、损害后续交付的问题，但有限束宽也意味着结果不是精确全局最优解。

接着，采用两阶段自适应大邻域搜索（ALNS）形成资源节约初值。第一阶段以 $(D,W,C_{\max},E_{\mathrm{tot}},N)$ 的顺序改进主方案；第二阶段要求 $D=0$ 且 $W$ 不超过第一阶段方案的 103%，在此范围内优先降低能耗。六类邻域操作分别为移箱、换箱、合并、拆分、访问顺序调整及移除重插；每 50 次迭代根据历史收益更新算子选择权重，并用模拟退火接受准则允许暂时较差的可行解。本次第一阶段运行 500 次，第二阶段使用三个固定随机种子、各运行 1200 次。

在 ALNS 初值上继续使用增量能耗引导的路线修复：为受影响的路线分别计算可行机型下的最低能耗，未变动路线保留当前能耗，据此排序跨架次移箱、合并和访问顺序调整。该估计不构成全局能耗下界；每次接受方案前仍重新安排实体机、共享电池、充电和架次开始时刻，并检验逐箱时限。对固定路线集合再用随机种子固定的执行顺序扰动，搜索能源与时限均合格的方案。快速分支要求最晚返航不超过既有 ALNS 方案的 7,531.737 s，且逐箱硬截止至少保留 60 s；资源节约分支要求零软延误，加权交付不超过旧主方案的 103%。最后在原主方案和本轮三个新增方案中按既定字典序选取主方案。

## 5 计算结果与取舍

表 1 列出九套经过同一物理与资源口径核验的完整方案。表中的“最晚返航”即本问的全部任务完成时间；加权指标的单位为秒乘以货箱优先系数。

**表 1 第二问运输调度方案比较**

| 方案 | 加权软延误 | 加权交付时刻 | 最晚返航 / s | 能耗 / kWh | 架次 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 交付优先，当前主方案 | **0** | **2,730,086.799** | 8,027.342 | 67.606 | **22** |
| 快速方案 | **0** | 2,746,630.503 | **7,531.737** | 71.587 | 24 |
| 最低能耗方案 | **0** | 2,811,170.769 | 8,027.342 | **66.961** | **22** |
| 上一版主方案 | **0** | 2,735,557.495 | 7,558.434 | 77.059 | 28 |
| 零延误较省能耗 | **0** | 2,800,522.364 | **7,531.737** | 76.401 | 27 |
| 加权交付优先搜索所得 | 10,639.753 | 2,771,259.248 | 7,629.680 | 73.210 | 25 |
| 限时节能 | 72,248.943 | 3,203,874.303 | 9,345.191 | 67.929 | 23 |
| 限时节能的时序改良 | 30,223.122 | 3,094,451.202 | 9,377.377 | 67.768 | 23 |
| 两阶段 ALNS 零延误节能 | **0** | 2,809,611.621 | **7,531.737** | 74.337 | 25 |

当前主方案采用 22 架次；8 架实体机和 14 组共享电池均参与任务，电池按机型分别使用。80 箱均完成交付，31 箱有硬截止的货箱全部按时送达，最小硬截止余量为 815.603 s；其余货箱也均未超过期望时间。最晚一箱交付于 7,521.955 s，最后一架运输机于 8,027.342 s 返抵 O01。架次编号、路线、逐箱送达时刻，以及每架实体机和每组电池的飞行与充电区间，分别见 `results_q2/q2_transport_sorties.csv`、`results_q2/q2_box_deliveries.csv` 和 `results_q2/q2_resource_timeline.csv`。

与上一版主方案相比，当前路线修复流程的主方案在保持零软延误的同时，减少 6 架次、节省 9.454 kWh，加权交付时刻减少 5,470.696 s·权重；其最晚返航增加 468.908 s。由于加权交付时刻在目标顺序中先于最晚返航，新方案仍是本流程已找到候选中按所设字典序更优的方案。若更看重任务完成时间，快速方案在 7,531.737 s 完成，能耗 71.587 kWh，硬截止最小余量 247.682 s。若更看重能源使用，最低能耗方案在 22 架次下耗电 66.961 kWh，最晚返航与当前主方案相同，但加权交付时刻增加 81,083.969 s·权重。这三套方案需要按救援偏好选择。

仓库同时保存了独立的固定路线 CP-SAT 排程实验：沿用原 28 架次箱组与访问顺序，仅重新安排机型、实体机、电池和起飞时刻，得到加权交付 2,662,962.480 s·权重、最晚返航 7,350.407 s、能耗 77.059 kWh、零软延误的可行方案。其加权交付优于本节 22 架次方案，按上述字典序也更优，但架次和能耗更高。相关结果位于 `results_q2_refined`，方法与求解状态见 `Q2_精确资源排程实验.md`。论文最终选用主方案时，应把两套结果放在相同口径下比较，不能仅凭本节表格宣称 22 架次方案是全部已知方案中的字典序最优。

表中既有的两套“限时节能”方案允许普通物资发生软延误，故不能仅凭能耗与架次数替代上述零延误方案。方案名称表示搜索侧重点，并不保证该行在同名单项指标上优于所有其他行；例如早期“加权交付优先搜索所得”的加权交付值仍高于当前主方案。这反映有限搜索所得可行解之间的比较，不能据名称推断最优性。

## 6 可行性核验与适用范围

对输出方案逐架次重新核对箱号、目的地、载质量、体积、DEM 航段、飞行阶段、分段能耗、返航 SOC 和交付时刻；再检查实体机及同型电池编号、占用区间、两阶段充电完成时刻、逐箱硬截止和目标汇总。主方案与八套备选均通过核验，且 80 箱各出现一次。结果目录中的 `q2_validation.json` 记录主方案 22 架次、31 箱硬时限和 8 套备选的核验数量；`q2_deadline_audit.csv` 可逐箱追溯交期和余量。

上述数值建立在附件数据、DEM 与本文明确采用的水平及爬升能耗公式上。路线邻域搜索、束搜索、ALNS 和增量修复均有有限预算，因而本文只给出经过回放检验的可行方案及其取舍，不宣称第二问的全局最优值。第三问需要重新加入连续通信约束，不能直接把本问运输方案当作最终通信可行方案；本问对照方案可作为第三问联合优化的不同初值。
