# D题第四问：论文技术路线、精确分区与资源配置实现建议

日期：2026-09-24。本文为方法调研与实现设计，尚未获得第三问最终方案，因此不报告第四问的新数值最优解。原始题目数据、论文和求解代码均未修改。

## 1. 推荐结论与最优性范围

推荐主路线：**固定第三问已核验方案 → 运输与中继任务超图 → 不可拆连通分量 → 无重复精确分区 → 分类型资源最小配置 → 峰值、接续链与匹配证书 → 双口径分别选优及完整权衡 → 独立回放。**

附件TRD-OCFG适合借鉴资源与时序耦合、冲突解释及破坏重组思想，不能直接用其“重叠联盟博弈”替代本题固定任务后的分区求解。第四问要求服务区唯一归组、任务期间资源不跨组调配；论文允许一架无人机参与多个任务联盟，并联合改变任务序列，两者的决策域不同。

第四问可以争取严格证明的结论是：在固定第三问任务、时序、通信关系及明确的资源配置政策下，遍历全部合法分区，给出目标政策下的精确最优方案或全部非支配方案。该结论不等于第三、四问联合全局最优，也不表示第三问本身最优。

用户给出的4个超节点、两组7种、三组6种、30/34资源单位，只能作为某个第三问方案的待复核结果。最终第三问改变后，以上数字均须重算。

## 2. 原题与现有项目核对

核对原题“问题四”原文：保持第三问货箱组批、访问顺序、运输与中继任务安排及通信保障关系不变；15个服务区恰好归属于2组或3组，每组非空；同一运输架次涉及的服务区同组；各组独立承担运输与通信任务，各类资源不跨组调配。原题允许报告超过库存的配置及缺口，因此“合法分区”与“现有库存可实施”要分开。

v4第9节阶段D已要求：第三问最终关系固定后重新生成任务块；保留两组、三组精确穷举；原编号口径与同类资源重新编号口径分别选优；输出缺口向量、均衡指标与具体设备分配。v4第10节明确，第四问精确性只针对固定第三问形成的任务块。

本轮核对的项目副本：`github-q3-delivery/q4.py`、`v3.md`、`交付说明_v4.md`，对应前轮发布Q3调研时的仓库副本，未把它称为本轮重新拉取的最新远端代码。

当前实现已有运输与中继关联、并查集、规范化分区、原编号数量、区间最大重叠与库存缺口。需要补齐的是：

| 位置 | 当前状态 | 推荐升级 |
|---|---|---|
| `coupled_components` | 同运输架次及同中继架次实际保障关系都参与合并 | 保存每条关联来源、实际服务区间及合并证据；先核验通信日志 |
| `resource_need` | 用峰值计数计算重新编号最低数量 | 输出具体资源接续链，并以独立最大匹配复核 |
| `solve`的`score` | 按原编号缺口、额外资源、工作量CV和空间离散度排序 | 两种政策分别遍历选优；保留类型向量与非支配方案 |
| `feasible=True` | 结构合法即标可行，即使有库存缺口 | 分别输出结构合法、任务回放通过、库存满足及增配后可执行 |
| 中继筛选 | 未被通信表使用的中继架次被排除 | 标记孤立任务；固定Q3后不得默默删任务，需确定归属或回Q3形成新版本 |
| 核验 | 复用资源计算函数 | 采用独立关联图遍历和独立匹配核对所有候选及备选 |

## 3. TRD-OCFG论文的实际技术路线

文献信息：Jiaxuan Li等，*TRD-OCFG: A Multi-UAV Overlapping Coalition Formation Game Oriented to Temporal and Resource Deadlock Constraints*，IEEE Internet of Things Journal，13(14)，2026，31431–31448；DOI：10.1109/JIOT.2026.3687018。已读取18页全文的主体、实验和附录，并对关键公式及算法页作图像核对。

### 3.1 问题模型

每个目标包含搜索C、物资配送S和复查V三个顺序子任务；异构无人机提供可消耗物资和非消耗感知资源；同一无人机可以先后参与多个联盟。决策同时包括资源分配、参与哪些联盟及执行顺序。

式(2)–(13)将时效收益、资源匹配程度和插入任务的额外飞行代价综合成效用，并最大化联盟总效用。资源不足的任务在论文中可以用低效用表示，任务完成率因而是实验指标。本题要求全部货箱及硬约束满足，不能把漏交或失联改为低效用。

主要假设是二维恒速飞行、续航简化为最大航程、稳定连通的通信图，不处理本题三维DEM与两跳链路的连续时间认证。

### 3.2 四个算法模块

| 模块 | 实际方法 | 对本题的用途 |
|---|---|---|
| 联盟构建 | 按需求缺口、资源匹配与任务插入代价形成概率，加入任务联盟 | 可启发Q3的候选路线/任务插入排序 |
| 冗余删减，算法2 | 撤出多余资源与成员，同时考虑资源需求和无人机数量 | 启发资源冗余诊断，但Q4应以精确最低配置代替随机删减 |
| 时序过滤，算法1 | 建立任务顺序图，随机改序以减少有向环 | Q4只作顺序/依赖核验；不能改动已固定的任务时序 |
| 死锁重组，算法3 | 释放并重建被卡住的任务联盟，结合重启和禁忌表跳出局部停滞 | 适合Q3外层破坏重组；Q4只可在完整任务块层面换组 |

算法4将以上模块置于异步、无领导者的分布式通信迭代中。论文以势博弈与Nash均衡讨论稳定性；这不证明其得到全局效用最大值，更不能直接证明本题分区最优。

实验通常采用200次独立Monte Carlo模拟，与PGG-TS、HOM、MCC-RMCF比较效用、任务完成率和迭代数，并做模块消融与静态通信延迟实验。其改进百分比属于论文自己的场景和效用，不能直接写成本题预期收益。

### 3.3 迁移时需要检查的细节

1. 第7页式(15)展示按资源贡献比例分配效用，不能仅凭论文使用“Shapley”一词就把该比例式作为一般合作博弈的Shapley值计算。本题若引入合作成本分摊，需要重新定义特征函数及边际贡献。
2. 第9页式(26)显示：剩余续航满足最小插入代价时，`flag3`取0；它与后续“保障续航满足”的解释存在疑点。且式(22)的softmax中得分为0仍对应正概率，因此这些标志本身不是不可行候选的硬过滤。若迁移算法，应独立禁止不可行选择，不能照抄并声称有可行性保证。
3. 算法1达到迭代上限也可退出，退出后仍需检查有向环是否清零；重启策略能改善搜索，不保证有限预算内找到全部任务可行解。
4. 第11页的复杂度分析主要围绕单机各模块；本题不能把其概括的复杂度直接当作完整异步多轮求解总成本，仍需计入循环次数、图检测、通信和独立核验。

这些检查限定公式的迁移方式，并非据此否定整篇论文。

## 4. 第一步：建立任务超图并压缩

设服务区集为V。每个运输架次p对应一个超边：

\[
H_p^T=\{\text{该架次实际访问的服务区}\}.
\]

每个中继架次r对应：

\[
H_r^R=\bigcup_{p:\,r\text{在Q3中实际保障过}p}H_p^T.
\]

在固定中继架次不可拆、实际通信关系固定且禁止跨组共用资源的解释下，`H_r^R`也必须同组。即使r在不同时间分别服务两个运输架次，它仍是同一个固定中继任务，不能在Q4中拆给不同组。

对所有运输、中继超边做并查集，得到连通分量C1,…,Cm。再用独立DFS/BFS重建连通分量，核对两个算法输出。

必须区别三件事：

- 同一个运输或中继架次连接的服务区：属于任务绑定。
- 同一实体无人机先后执行两个独立架次：在允许重新配置同类资源的口径下，不自动绑定服务区。
- 一个中继点理论上可覆盖某服务区：不是实际通信保障关系，不能据此合并。

例如运输p1访问S01、S02，p2访问S03，同一中继r实际保障p1和p2，则三者同组。如果只有同一实体运输机执行p1、p2，而两任务没有其他关联，则重新配置口径下仍可以分组，但需要组间独立设备。

每个连通分量应输出证据链，例如“S01—运输T05—S02—中继R02所保障的运输T09—S03”。只给分量列表不足以定位通信表导致的误合并。

压缩具有等价性：合法分区一定不能拆开任何超边及其传递闭包；反过来，将整个分量分到同一组即可满足这些不拆分限制。因此可在m个分量上精确搜索，且不会因压缩丢失合法分区。

## 5. 第二步：无重复枚举分区

两组、三组均要求非空、组名无区别。合法候选数为第二类Stirling数S(m,k)：

\[
S(m,2)=2^{m-1}-1,\qquad
S(m,3)=\frac{3^m-3\cdot2^m+3}{6}\quad(m\ge1).
\]

若m=4，确为7种和6种；若m=5，则是15种和25种；m<k时相应分区无解。不能为了获得三组在Q4中拆开已固定中继任务，应回到Q3生成另一个明确标识的兼容版本。

使用规范化标签序列生成分区：第一个分量属于组0，后续标签最多比已用最大标签大1，最终恰有k个标签。用Stirling递推独立核对枚举数量并检查没有重复。

m最多为15。若任务绑定较少，S(15,2)=16383、S(15,3)=2375101，可先为不超过32767个非空分量子集缓存组内资源需求，再枚举分区做合并评价。标量且可加目标也可采用固定包含最小元素的子集动态规划；多目标时需要保存非支配标签并说明标签规模，不能把近似删标签的结果称全局精确。

## 6. 第三步：分开计算两种资源政策

### 6.1 固定任务与时序，允许同类设备重新配置

该口径直接回答“独立执行这些固定任务最少需要多少设备”。前提：同类资源可互换、所有任务起终点一致、初始能源充满、每次再用前充满、充电并行且没有额外充电器容量或跨资源配对限制。与本题不一致的额外假设不得自行加入。

资源必须分八类：A/B/C运输机、A/B/C电池、中继机、中继能源组件。不同机型的机身和电池不得互换。

| 资源 | 固定占用区间 |
|---|---|
| A/B/C运输机 | 架次准备开始至返航完成；若题目另有机身周转要求，计入其释放端点 |
| A/B/C共享电池 | 电池实际投入任务至返航后充至100%；与Q3准备/装箱阶段的电池占用口径一致 |
| 中继机 | 准备开始至返航并完成机身周转 |
| 中继能源组件 | 投入任务至返航后充满；按题目明确的组件规则计算 |

机身周转与组件充电是否并行，要沿用题目和Q3经过核验的公式，不自动串联。不能只统计空中飞行阶段，不能将电池充电时间计入运输机占用。

对组g、类型r的任务区间[a_i,b_i)，最少配置为：

\[
n_{g,r}^{*}=\max_t\sum_{i\in I_{g,r}}\mathbf1\{a_i\le t<b_i\}.
\]

下界：峰值时刻同时占用的任务必须使用不同资源。上界：按开始时刻排序，用最早释放资源优先分配，可恰好用峰值数量执行全部区间。上下界闭合，得到该组该类资源的精确最小值。

实现时采用半开区间，允许一项任务在t释放后另一项在t立即开始时，结束事件先于开始事件。数值容差和时间精度必须统一；若额外增加安全间隔，应另列稳健场景，不能暗中改变主结果。

八类资源可以分别求解的原因是固定任务下已无额外的机身—电池永久绑定；各类分配完成后按架次组合即可。若存在指定兼容性、位置转移、充电器容量、不同初始SOC等额外约束，峰值一般只提供下界，需要另建匹配/流/整数规划模型。

### 6.2 保留原编号使用模式的独立配置口径

逐组统计Q3原机身、电池等编号数量，保留该组原有任务接续关系，得到一个可执行的配置上界。它可能比重新分配同类设备需要更多资源，两种政策必须分别选优。

同一旧编号出现在不同组时，应明确需要不同的物理副本，并输出不同的新编号，例如G1-A01、G2-A01。不能写成同一台A01仍跨组执行。

若“原编号不变”被解释为绝对保留同一物理实体且禁止复制，那么共享该实体的任务还会产生额外同组限制；那是更严格的另一模型。报告应明确本项目原编号政策表示保留分组后的使用模式及复制配置，避免语义混淆。

## 7. 第四步：最大匹配与接续证书交叉核验

对每组、每类型，将n个任务复制为左右两份节点。当b_i≤a_j时连边i_L→j_R，求最大匹配M*。最少资源接续链数为：

\[
n_{g,r}^{*}=n-|M^{*}|.
\]

每条链对应一个具体实体资源，链内任务在资源准备好之后再接续。固定区间的“可接续”关系构成偏序；Dilworth定理说明最小链分解数等于最大反链大小；区间情形的最大反链大小等于最大同时占用数。

因此本题可以做两套独立计算并共享一个理论证明：扫描线峰值计数；二分图最大匹配及接续链构造。Dilworth是理论依据，不宜包装为第三个独立数值算法。若连接关系加入依赖前后任务的转换限制，需重新检查偏序及区间条件，不能机械套用三者相等。

每种资源交付：最低数量、峰值时间及同时占用任务、最大匹配、接续链、设备编号安排。独立核验器重读端点与类型，检查任务覆盖一次、无重叠、能源再用前充满、组间编号不共享。

## 8. 第五步：资源规模、缺口与均衡分别评价

对分区P，在同一种资源政策下定义：

\[
N_r(P)=\sum_g n_{g,r},\qquad
\Delta_r(P)=[N_r(P)-S_r]_+,
\]
\[
L_r(P)=N_r(P)-n_{\mathrm{pool},r},\qquad
R_{g,r}=A_{g,r}-n_{g,r}^{*}.
\]

S_r是全系统库存，n_pool,r是未分组的统一执行需求，A_g,r为实际选定配置。四者分别是总需求、库存缺口、分组造成的共享损失、配置富余；不能统称冗余。只有实际分给组g的库存才能用来评价该组缺口，不能让每个组分别与全部库存比较。

保留八维需求和缺口。不同机型、机身和电池各算一个单位，只是透明的等权统计，不代表采购成本。若缺少价格，展示资源向量的非支配前沿，并将等权资源总数作为明确声明的辅助政策。

工作量可用累计运输任务时长加累计中继任务时长计算CV，同时列出箱数、质量、运输架次、中继架次和各类资源峰值。相同任务时长对不同资源意味着不同压力，单一CV不够；任务准备、飞行和服务计入范围应与Q3一致。各组工作量之和、任务能耗之和在固定任务分区下保持不变，可用于守恒检查。

建议同时输出：资源需求较小的非支配方案、最均衡方案、指定均衡阈值下的最少资源方案、库存内可执行方案或最小缺口前沿。两种资源政策各输出一套，不能先按原编号挑出一组再给它附上重新配置后的数量并称后者最优。

题目没有规定地理连通时，不把地理连通强加为硬约束；距离或空间离散度用于解释和辅助选优。

## 9. 两个适合形成论文贡献的精确分析

### 9.1 分组导致资源共享损失的证明

若P'是P的进一步细分，则对每类资源：

\[
N_r(P')\ge N_r(P).
\]

对重新配置口径，这是“各子组峰值之和不小于合组峰值”；对保留使用模式口径，这是“各子组编号数之和不小于编号并集大小”。所以细分不会降低逐类最低资源需求，可能保持相同，也可能增大。

进一步，任何三组合法方案都可以合并两组成为合法两组方案；若目标是非负权重的资源规模或库存缺口，且没有另加妨碍合组的硬约束，则两组的最优标量值不大于三组。加入均衡阈值等限制后须重新判断，不能无条件延伸。

这说明30与34可出现的机制，但不能替代对用户方案的复算。不同的两组和三组均衡解也可能互不支配，应完整报告资源向量。

### 9.2 同样最低数量下，优化接续时间余量

对连续复用边i→j定义间隔d_ij=a_j-b_i。固定最少资源数r*后，在所有大小为n-r*的匹配中最大化最小选中边间隔：

\[
\max_{M:\,|M|=n-r^*}\min_{(i,j)\in M}(a_j-b_i).
\]

对候选间隔阈值δ，仅保留d_ij≥δ的边；若仍有大小n-r*的匹配，则该资源数下可实现至少δ的接续余量。在有限临界间隔上搜索得到精确最优值。没有复用边的类型记“不适用”，不拿无穷余量参与整体评分。

该分析可在不改变Q3时间、路线和设备数量的条件下，挑出对单次周转变慢更宽容的编号安排。它不保证连续通信对任意延误依然成立，也不能替代货箱时限和多阶段延误传播分析。

另可做占用释放端点统一延长δ的资源敏感性分析，得到数量跳变阈值；需注明这是附加稳健情景。

## 10. 第三问尚未结束时即可完成的准备

先实现纯后处理接口，输入一个不可修改的Q3快照：

- 运输架次：ID、机型、货箱、访问次序、准备/起飞/返航时间、逐箱交付、能耗及电池再次可用时间；
- 中继架次：ID、悬停点、准备/建链/服务/返航时刻、机身与组件再次可用时间；
- 实际通信保障区间：运输架次、中继架次、区间端点和认证状态；
- 类型库存、初始条件、资源政策、输入摘要与Q3核验状态。

先以人工小例测试：仅运输连接、仅中继连接、传递合并、同机不同架次不误合并、理论覆盖不误合并、m<k、端点相等、跨机型电池、充电未完成、孤立中继、库存不足、同编号跨组及保守时间缓冲。

第三问最终输出后，仅替换输入快照并重新运行；不得保留旧的分量数或最优分区。若第三问多个非支配候选都可接受，可分别评估第四问资源代价供最终方案选择，但每个候选都要单独固定，且第三问性能牺牲须明示。

推荐实现顺序：

| 阶段 | 工作 | 验收证据 |
|---|---|---|
| A | 输入校验、任务关联及合并来源 | 并查集与独立图遍历一致，无漏任务 |
| B | 八类资源区间与最低配置 | 峰值、匹配、分配链三类证据一致 |
| C | 全部无重复两组/三组分区 | 枚举计数与Stirling数一致 |
| D | 两种政策分别选优与非支配档案 | 逐类需求/缺口、均衡及配置富余 |
| E | 时间余量与分组代价分析 | 接续余量阈值、共享损失解释 |
| F | 接入已核验Q3最终版本 | 所有正式和备选分区独立回放通过 |

建议交付：`q4_components.json`、`q4_coupling_evidence.csv`、`q4_all_partitions.csv`、`q4_pareto.csv`、`q4_resource_assignments.csv`、`q4_peak_witnesses.json`、`q4_matching_certificate.json`、`q4_slack_frontier.csv`、`q4_validation.json`及`manifest.json`。这些是待实现产物，本轮未声称已生成。

## 11. 最终推荐的论文表述

“在第三问已核验的运输、中继任务与通信保障关系固定的条件下，构造服务区任务关联超图，并通过连通分量压缩获得不可拆任务单元。对两组、三组所有合法分区进行精确搜索。按资源类型建立包含任务、周转与充电的占用区间，通过峰值下界与具体分配构造求得最小配置，并以二分图最大匹配进行独立核对。在两种资源配置政策下分别比较库存缺口、资源共享损失、工作量均衡及接续时间余量，给出可执行配置与相应最优性证据。”

“精确最优”只能用于已声明的固定任务模型和目标政策；多目标若未指定偏好，则输出非支配方案，而不是宣称唯一综合最优。

## 12. 核对来源

- 用户附件TRD-OCFG，PDF第3–10页模型与算法，第11–16页实验，第16–17页附录证明；重点图像核对第7、9、10、17页。
- 原始题目DOCX“问题四”及附录2的资源充电周转规则。
- 仓库副本`v3.md`第5节、`交付说明_v4.md`第8–10节、`q4.py`及`Q3_方法调研.md`。
- 区间划分的峰值下界和贪心最优构造：[Princeton课程讲义](https://www.cs.princeton.edu/courses/archive/spr05/cos423/lectures/04greed.pdf)。
- Dilworth链分解原理：[MIT代数组合学讲义](https://ocw.mit.edu/courses/18-212-algebraic-combinatorics-spring-2019/688d7bea866de628c9cb1dceb9c1bb06_MIT18_212S19_lec17.pdf)。

本文新增的接续余量优化、分组代价与独立核验设计是针对赛题的推导和建议，不是声称附件论文已经提供这些结果，也未作“首次提出”的新颖性声明。
