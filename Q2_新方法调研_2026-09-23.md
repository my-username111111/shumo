# 第二问下一轮优化调研：从随机改路线转向精确资源排程

日期：2026-09-23。本文只做现有结果审查和文献调研，**没有产生新求解结果**，也没有把论文中的改善幅度套用到本题。数值取自当前仓库 `results_q2/q2.json`；方案口径见 `Q2_交付说明.md`。

后续已按本备忘录的路线 A 完成固定路线精确排程实验；实测数值和可核验交付见 `Q2_精确资源排程实验.md`。

## 1. 现有方案已经做到什么

| 方案 | 加权软延误 | 加权交付时刻 | 最晚返航/s | 能耗/kWh | 架次 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前主方案 | 0 | 2,735,557.495 | 7,558.434 | 77.059 | 28 |
| 两阶段 ALNS 零延误节能方案 | 0 | 2,809,611.621 | 7,531.737 | 74.337 | 25 |
| 限时节能的时序改良 | 30,223.122 | 3,094,451.202 | 9,377.377 | 67.768 | 23 |

主方案的加权软延误已经达到非负指标的下界 0；下一步须在零延误条件下改善加权交付、完工时间、能耗或架次，或者清楚报告这些指标的非支配权衡。不能再把“把软延误降到 0”作为新成果。

当前代码已试过搬箱、换箱、合并、拆分、访问重排、2–4 箱移除重插，以及 64 状态束搜索。重复扩大相同随机算子的迭代次数，缺乏针对当前瓶颈的依据。

## 2. 从代码与结果找到的空白

1. **路线候选的调度评价仍偏贪心。** `q2_alns.py::_search` 每个候选调用 `dispatcher.schedule(..., attempts=2)`；这个函数对给定路线顺序逐架次选当前最优的实体机、电池和起飞时刻。联合束搜索只在 `q2.py::solve` 的一个阶段使用，并没有作为每个 ALNS 候选的严格排程器。这意味着某个路线集合可能因两次贪心排程不好而被淘汰；这是代码机制推断，尚未通过新实验量化。
2. **现有束搜索的截断分数没有剩余任务下界。** `schedule_beam` 在同一深度把剩余任务集合不同的状态直接按已交付部分的目标排序，保留前 64 个。状态虽然都排了相同个数的任务，但货箱优先系数、截止时间和难度不同，已发生费用并不可直接比较。应在新实验中用可采纳的剩余费用下界或按剩余集合分组，观察结果是否改变；目前不能断言这已经造成了多少损失。
3. **搜索明显停滞。** `results_q2/q2.json` 中，ALNS 第一阶段 500 次迭代未刷新最优；第二阶段三个种子各 1,200 次，分别仅接受 3、2、3 个候选。这个现象比“种子不够多”更支持优先改善候选修复与评价机制。
4. **关键时段主要受实体机约束。** 当前 25 架次 ALNS 方案首轮 8 架均在 0 秒出发。其余 17 次起飞中，按当前所选资源的最早可用时刻复算，14 次由无人机返航时刻卡住，3 次由电池充满时刻卡住。这只是对已选方案的诊断，不表示电池约束可删除；它提示完工时间邻域应优先处理晚返航无人机的任务链。

## 3. 真正不同于当前实现的四条路线

### A. 先把**固定路线集合**的排程交给 CP-SAT，建立清晰基准（优先级最高）

固定现有 25 架次 ALNS 方案和 28 架次主方案的“箱组＋访问顺序”，允许每架次重新选择可行机型、实体机、电池及起飞时刻。为每架次设运输机占用区间 `[t, t+航次时长)`，为被选电池设占用区间 `[t, t+航次时长+充满时间)`；同一实体机和同一电池分别用 `NoOverlap`。每箱交付时刻是 `t+路线内相对交付时间`，硬截止和零软延误直接加约束。采用顺序求解实现真正的字典序目标：先锁定软延误 0，再最小化加权交付，之后完工时间。对节能方案则固定零延误和明确的交付预算，再最小化能耗；固定路线集合的架次数不会变化。

这一步只改变调度，不改路线，因此能回答最基本的问题：当前差距到底来自资源排程还是路线组合。若固定路线排程找到更好解，可直接用现有 `verify_q2` 独立回放；若求得该固定路线集的最优界而无改善，再把投入转向路线生成。离散化时须说明时间粒度；保守地向上取整飞行与充电时长、向下取整硬截止，输出再用连续时间物理核复验。离散模型的最优证明只对所选粒度和固定路线集合成立。

依据：Naderi 与 Roshanaei 的[关键路径 CP–逻辑 Benders 排程研究](https://pubsonline.informs.org/doi/10.1287/ijoo.2021.0056)显示排程子问题可通过 CP 与分解求解；[OR-Tools 官方区间排程示例](https://developers.google.com/optimization/scheduling/job_shop)展示了 `NoOverlap` 的建模方式。前一篇研究对象是柔性车间，非本题无人机；借鉴的是双资源排程结构，改善须在本题复算。

### B. 保存并重组历次搜索的路线，而非只保存整套方案

将六套已验证方案及后续 ALNS 的每条可行路线汇成“箱集合＋访问次序＋可行机型”的去重路线池。主问题为每箱恰好由一条路线覆盖：`sum_{r: b∈r} x_r = 1`。对选出的路线集合，再用 A 的排程器检查实体机、电池、硬截止和零软延误。若路线组合无法排程，记录冲突并回传禁止组合或更强的资源冲突割。这样可以组合不同历史方案中的优良局部路线，而不是等待随机搬箱一步步走到完整方案。

如果仅优化路线能耗，主问题能给当前有限路线池的松弛下界；但忽略时序的路线组合不能直接称为可行解，有限路线池的最优也不是原问题全局最优。研究依据是 Dumez 等的[大邻域搜索＋精确集合划分重组](https://doi.org/10.1016/j.ejtl.2021.100040)、Naderi 与 Roshanaei 的排程分解，以及 Safdari Shadlou 等对[灾后无人机调度应用逻辑 Benders](https://doi.org/10.1016/j.cie.2023.109542)的研究。最后一篇还涉及道路抢修，不能直接照搬其约束。它们支持方法选择，不保证本题数值收益。

### C. 用资源关键链引导破坏与修复

从最晚返航架次，沿“此架次为什么不能更早开始”的无人机或电池前驱追溯关键链。一次移除关键链上的 2–3 条架次及相邻截止时间货箱，精确枚举或小规模 CP 重建该局部，保留其余已确认方案。用于加权交付时，同样从贡献最大的晚交箱及其资源前驱出发。这个邻域与当前随机抽 2–4 个货箱不同，它按造成延误的因果链选变量。可先在固定路线集上试，再联动箱组。

借鉴[关键路径搜索的逻辑 Benders](https://pubsonline.informs.org/doi/10.1287/ijoo.2021.0056)与[多架次车辆路径的客户、路线、工作日多层邻域](https://doi.org/10.1016/j.cor.2013.08.016)。关键链识别适合本题的证据来自本仓库资源时序诊断；收益仍待实验。

### D. 若 A–C 仍不能改进，再考虑定价生成路线与严格下界

以路线集合划分为主问题，用带载荷、体积、能量、硬截止和交付时间资源的标签算法生成负约化成本路线，再让排程子问题检查有限无人机与电池。此方向接近分支定价／分支检查，可以为“离更优方案还有多远”提供下界，但开发量和数值稳定性要求高；80 个箱、15 个服务区的本题不宜一开始就全面实现。研究参照[带时间窗的无人机路径分支定价](https://doi.org/10.1016/j.ejor.2023.02.030)及[多架次无人机时间展开网络分支定价](https://doi.org/10.1016/j.trb.2024.103117)。前者含卡车，后者服务移动船舶，其模型不能原样套用。

2025 年[有限电池的路径与充电联合优化论文](https://doi.org/10.1016/j.trc.2025.105082)还提出专门的电池调整算子。当前代码虽显式排电池，却没有围绕电池充电冲突做针对性邻域；若 A 的排程证实电池瓶颈显著，再把该算子列为优先项。目前 25 架次方案的 17 个后续起飞中仅 3 个受电池最早可用时刻卡住，因此不能直接假定它是当前最大增益点。

## 4. 建议的最小实验与验收

1. **只做固定路线精确排程实验。** 对现有主方案、25 架次 ALNS 方案分别给相同计算时限。记录求解器状态、可行上界、目标下界、时间粒度、运行时间和实际回放指标；不改原始物理模型。
2. **明确成功指标。** 主方案要求零软延误、80 箱及 31 个硬截止箱全部合规，并使加权交付时刻严格低于 2,735,557.495；节能对照要求零软延误、加权交付不超过明确预算，并与 74.337 kWh、25 架次、7,531.737 秒逐项比较。一个指标改善而其他指标恶化时，报告 Pareto 权衡，不直接称“全面更好”。
3. **若固定路线无改善，再做路线池重组。** 对所有候选保留来源、箱号、机型、相对交付时刻、能耗和充电时间；集合划分的每个候选必须通过双资源排程和独立回放。有限池结果注明池大小和求解 gap。
4. **对照要公平。** 与现有 ALNS 使用相同物理口径、机器、时间预算和多随机种子，分别报告最优、均值、标准差和不可行率；任何新结果保存逐箱交付表、架次表、资源时序和 `verify_q2` 证书。

**当前结论：最值得先验证的不是再增加一个随机算子，而是把现有好路线的实体机与共享电池排程求得更准。** 若这一步没有空间，路线池重组和关键链局部重建才有清晰的价值定位。
