# D题第三问运行结果（连续通信升级版）

本文保留首次连续核验阶段的 24+9 架次基线。后续联合排程、路线反馈和独立核验的最新交付见 `Q3_交付说明.md` 与 `results_q3_complete`；以下“本轮”“未完成”均指该历史阶段。

## 1. 本轮结论

本轮已在最新 Q2 项目上实现并运行第三问升级核。当前得到一组**经过连续区间证书和独立逐秒回放的可行上界**：24 个运输架次、9 个中继架次，通信中断 0 秒，未知区间 0 秒。

该解不能称为第三问最优解，也没有优于“21 个运输架次 + 3 个中继架次、7779.2 秒、73.10 kWh”的外部对照。它的价值是首次把连续通信、两架实体中继机、六套能源组件、返航余量、周转与充电放在同一时间轴上并全部通过复核。后续优化应从这组严格可行上界继续压缩，而不能退回“逐秒抽样为零中断即视为连续可行”的口径。

## 2. 最终可行方案指标

| 指标 | 结果 |
| --- | ---: |
| 运输架次 | 24 |
| 中继架次 | 9 |
| 总架次 | 33 |
| 加权软延误 | 1,416,548.691 优先级·秒 |
| 加权实际交付时刻 | 5,438,318.768 优先级·秒 |
| 联合最晚返航 | 16,304.917 秒 |
| 运输能耗 | 69.154516 kWh |
| 中继能耗 | 5.700338 kWh |
| 总能耗 | 74.854853 kWh |
| 累计运输任务时间 | 35,583.297 秒 |
| 直连时间 | 19,970.298 秒 |
| 中继保障时间 | 15,612.999 秒 |
| 中继占比 | 43.8773% |
| 连续证书最小余量 | 0.014498 dB |
| 连续中断 / UNKNOWN | 0 / 0 秒 |
| 独立逐秒回放样本 | 35,695 |
| 独立回放最小采样余量 | 0.001329 dB |

独立回放同时检查了逐箱覆盖、硬截止、运输机、电池、中继机、能源组件和中继能量，均无错误。当前最小余量很小，固定方案不能承受 1 dB 附加损耗；正式论文应将其写成名义参数下可行解，并把提高通信余量列为下一轮优化目标。

## 3. 为什么没有直接沿用最新 Q2 时刻

最新 Q2 的 24 架次均衡方案为：加权实际交付 2,500,844.903，最晚返航 6,073.208 秒，运输能耗 69.154516 kWh。固定其所有起飞时刻后，连续链路模型产生 202 个中继需求区间。有限候选池集合覆盖在加入第一个“两架中继机”占用容量割后不可行；冲突集中在约 3225.91 秒，T006、T012、T013、T014、T015 同时执行，且下一站中继必须提前起飞。

这只证明“当前候选池 + 固定 Q2 时刻”无解，不能证明原连续问题无解。它足以否定一种常见做法：先把 Q2 压到最紧，再在末尾补中继。第三问必须让运输时序或路线向通信资源反馈。

本轮又试验了保持箱组和路线、只错峰若干运输链的方案。该方案保持硬截止和零软延误，但在 11 个中继容量割后仍不可行。因此最终采用旧快速调度器产生的可行运输波次作为初值，再用新的连续核验和 0–1 中继任务模型重算；最后把全程直连且资源空闲的 T001、T004 提前到 0 秒，降低软延误而不改变中继排程。

## 4. 已实现的方法

1. **静态精确视线。** 射线逐个穿越 DEM 闭像元，比较像元地形与像元内最低射线高度，不再用固定 15 米采样作为正式判据。
2. **连续运动区间证书。** 固定网关或中继点与运输机运动航段形成扫掠三角形；射线高度在三角形上是仿射平面。程序逐行枚举相交像元，并在真实三角形—像元交集顶点上求最低高度。距离损耗取区间端点的可靠上界；若即使计入遮挡损耗仍满足门限，则直接认证整段。
3. **全域粗筛和航迹局部候选。** 在合法 DEM 区域按回传链路的理论最大半径先剪枝，再加入航迹、服务区和网关附近候选。当前正式运行使用 0.01° 全域粗网格、0.003° 航迹局部网格和 300 米离地高度，共保留 415 个回传可行站点。
4. **缺口子区间覆盖。** 每个运输阶段按事件分段，不能整段认证时细分至不超过 120 秒；不同子区间可以由不同中继任务接力。
5. **有限池整数覆盖。** 枚举中继服务任务，显式计入准备、飞抵、建链、悬停、返航、300 秒周转和组件充电。0–1 模型覆盖全部通信区间，并在每次发现三任务重叠时加入“两架中继机”容量割。
6. **独立诊断回放。** 独立程序使用公共模型的另一套逐点链路计算，以 1 秒分辨率复核 35,695 个时刻，同时检查四类实体资源和能量。

## 5. 与 Q3_方法调研路线的差距

本轮完成了实施顺序 A、B、C 的核心内容，以及 F 中的独立回放、固定方案鲁棒性和 Q4 耦合输出。仍未完成：

- 运输箱组、访问顺序与中继任务的统一 ALNS/CP-SAT 外层搜索；
- 入选中继点的几十米级水平细化和 100/200/300 米高度精修；
- 23、24、25 架次多个 Q2 初值在同一连续核验预算下的完整比较；
- 有效的有限池求解下界、相对间隙和原问题下界；
- 对 1/2/3 dB 附加损耗允许重新优化后的稳健方案。

因此 `q3_bound_report.json` 只把当前解称为可行上界。`q3_pareto.csv` 当前只有一条严格可行记录，不应当被描述成完整 Pareto 前沿。

## 6. 与外部 21+3 对照的结论

当前解在运输架次、中继架次、联合完工和总能耗上都没有超过外部对照：24+9 对 21+3，16,304.917 秒对 7,779.2 秒，74.854853 kWh 对 73.10 kWh。当前解的连续通信证明更严格，但在没有取得外部方案完整路线和同口径连续证书前，也不能据此断言外部方案不可行。

下一轮最值得做的是把“中继资源冲突割”反馈到 Q2 的路线池：优先破坏在 3225.91 秒附近形成三中继占用的 T006/T012/T013/T014/T015 组合，同时把通信余量至少提升到 1 dB。接受新解的硬条件应保持为：硬截止、四类资源、中继返航余量、连续通信证书全部通过；在此基础上按加权软延误、加权交付、最晚返航、总能耗和总架次比较。

## 7. 交付文件

- `q3_upgrade.py`：连续区间几何、候选生成、覆盖关系和中继任务整数优化。
- `build_q3_feasible_seed.py`：从 Q2 的 24 架次方案可复现地生成运输反馈初值。
- `run_q3_upgrade.py`：多初值/单初值运行与标准结果导出。
- `q3_independent_audit.py`：独立逐秒诊断和资源、能量复核。
- `results_q3_upgrade/q3_plan.json`：完整运输与中继方案。
- `results_q3_upgrade/q3_continuity_certificate.json`：连续通信证书摘要。
- `results_q3_upgrade/q3_independent_replay.json`：独立逐秒回放结果。
- `results_q3_upgrade/q3_communication_intervals.csv`：逐连续区间通信方式与余量下界。
- `results_q3_upgrade/q3_resource_timeline.csv`：运输机、电池、中继机和能源组件时间线。
- `results_q3_upgrade/q3_deliveries.csv`：逐箱交付结果。
- `results_q3_upgrade/q3_robustness.csv`：固定方案附加损耗诊断。
- `results_q3_upgrade/q3_q4_compatibility.json`：实际任务耦合得到的 Q4 超节点；当前为 3 个，其中一个包含 13 个服务区，分组均衡性较差。
- `results_q3_upgrade/q3_bound_report.json`、`q3_pareto.csv`、`manifest.json`：求解边界、可行档案和文件哈希。
