# Q3缩时优化及对Q4的影响

本轮以 `c8b7305` 中的 `results_q34_compromise/selection/primary` 为基准，完成受Q4资源与均衡限制的实际搜索。得到一个小幅增配设备的缩时备选：**完工提前292.759秒，约4分53秒，降幅3.93%**。

| 指标 | 原折中方案 | 缩时备选 |
|---|---:|---:|
| Q3联合完工时间/s | 7444.456 | 7151.697 |
| 总能耗/kWh | 69.330775 | 70.585965 |
| 加权送达时间/优先级·s | 2938847.284 | 2971140.383 |
| 普通物资加权软延误 | 0 | 0 |
| 运输/中继架次 | 24/4 | 24/4 |
| 两组资源缺口/件 | 4 | 5 |
| 两组工作量CV | 0.182786 | 0.189519 |
| 三组资源缺口/件 | 7 | 8 |
| 三组工作量CV | 0.276545 | 0.263027 |
| 硬时限箱按时交付 | 31/31 | 31/31 |
| 连续通信中断/s | 0 | 0 |

## 取舍与建议

两种分区的服务区成员均不变。各分区唯一增加的资源类型是**A型运输机，需求4→5架**；其余七类资源需求不变。两组设备总数31→32件，三组34→35件。Q3统一调度始终使用题目原库存；新增设备需求发生在Q4要求各组独立执行时。

两组CV只增加0.006733，工作量占比从59.14%/40.86%变为59.48%/40.52%；三组CV改善0.013518。能耗增加1.255191 kWh（1.81%），加权送达时间增加1.10%，但全部硬时限和普通物资期望时限仍满足。最后完工更早不代表每个货箱都送得更早。

因此，若允许Q4两组方案或三组方案各补1架A型机，可采用此备选；若要求资源缺口严格保持4/7件，则应继续使用原折中方案。本轮未找到严格不增缺口且满足相近能耗、交付和均衡指标的更快方案，没有将失败或限时未找到解解释为原题全局无解。

## 如何修改

1. `optimize_q3_timing.py` 增加 `makespan` 优先模式，首先压缩运输机和中继机的最晚返航时间；原先 `timely` 首先优化的是加权送达时间。
2. `q3_joint.py` 同时保护两组与三组分区的逐类型峰值、缺口和设备总数。合并组内部允许复用设备，独立组之间不能因工作时段错开而共享设备；电池占用计入充电。
3. `optimize_q3_time_guarded.py` 先搜索各类型数量不增，再搜索总缺口不增，最后生成最多增1件设备的备选。分别记录求解状态、界、限时及输入指纹。CV限制在连续复核后验收，不把求解器找到的候选直接当作交付。
4. 实际入选计划保持全部货箱组批及访问顺序，仅将S009的T022由A型改为C型，并重新分配起飞时间、实体机、电池及中继时窗。随后固定设备顺序精修连续时间，从7160.906秒压缩至7151.697秒。
5. 冻结该Q3，重新枚举并核验全部121个Q4分区。两组和三组都取同一份Q3的结果，指定分区见 `balanced_partitions.json`。

原模型的固定结构LP仍为7444.456秒。原组批下，无新增缺口且能耗/加权送达增幅上限为3%的加深搜索报告为INFEASIBLE；这只针对三处中继候选、给定组批、有限机型选项、1秒整数排程及本轮上限。它不能排除更细时间、其他组批或点位的改善可能。

## 验收、文件与复现

- 全量73项回归测试通过：`results_q3_time_guarded/regression_tests.log`。
- 全部80箱恰好交付一次，31个硬时限箱均按时，最小硬时限余量38.014秒；零软延误。
- 独立连续通信和Q4资源回放通过；额外1 dB损耗、至少5秒实体资源接续缓冲保留。
- [完整交付说明](results_q3_time_guarded/selection/one_extra/Q3缩时及Q4影响交付说明.md)：两组、三组资源向量与全部分区成员。
- `results_q3_time_guarded/selection/one_extra/q3_plan.json`：已认证完整计划。
- 同目录的 `comparison.csv`、`task_changes.json`、`transport_sorties.csv`、`relay_sorties.csv`、`box_deliveries.csv` 和 `communication_intervals.csv`：比较及执行明细。
- 同目录的 `q4/`：全分区、具体设备分配、库存映射、峰值与匹配证书、独立验收。
- `results_q3_time_guarded/experiment_inventory.json`：实际运行的搜索状态。生成但未运行的种子不作为实验结论。

复核已保存方案并重新导出：

```powershell
.\.venv\Scripts\python.exe deliver_q3_time_guarded.py --input results_q3_time_guarded/cp_one_extra/polished/q3_plan.json --output results_q3_time_guarded/replay_delivery
```

重做搜索：

```powershell
.\.venv\Scripts\python.exe optimize_q3_time_guarded.py --output results_q3_time_guarded/replay_search --resource-policy shortage --resource-allowance 1 --quality-tolerance .03 --seconds 65 --seed 2151
```

CP-SAT的阶段最优只针对有限候选模型；连续LP最优只针对固定结构。多线程限时搜索不保证重现相同启发式路径，本轮不宣称第三问全局最优。普通运行入口的历史默认方案与本备选不同，复算本轮请明确指定以上完整计划。
