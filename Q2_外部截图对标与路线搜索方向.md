# 第二问外部截图对标：下一步应改箱组与路线

本页只把用户提供的**裁切截图**当作待核查的候选结果，不把其中汇总数字当作通过本仓库物理核验的解。截图没有逐箱交付表、完整航线、实体机编号、电池充电时间轴或能耗计算式；括号中的秒数含义也不明确，因此暂不使用。

## 可读数字与我们的结果

| 来源与方案 | 架次 | 能耗/kWh | 最晚返航/s | 硬违反 | 软加权延误 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 截图行 1（标为推荐） | 24 | 71.436 | 6,577.2 | 0 | 0 |
| 截图行 2 | 20 | 63.241 | 9,205.2 | 0 | 0 |
| 截图行 3 | 25 | 72.246 | 6,444.0 | 0 | 0 |
| 截图行 7 | 20 | 63.974 | 7,516.8 | 0 | 0 |
| 我们的新主方案 | 28 | 77.059 | 7,350.4 | 0 | 0 |
| 我们的新节能方案 | 25 | 74.097 | 7,531.7 | 0 | 0 |

截图中的小时已乘 3,600 换算为秒。若截图行 1 通过**同一组** 80 箱、30 个首批箱、16 箱医疗物资、返航 SOC≥20%、A/B/C 实体机 4/2/2 架、同型共享电池充满后复用、DEM 航段及逐箱交付的完整核验，则它相对我们的主方案少 4 架次、少 5.623 kWh、最晚返航提前约 773 秒，值得认真追赶。但仅凭“硬违反 0”无法复核硬时限定义或资源可行性，不能把它直接写成已证实的优势。

截图推荐顺序是“及时性→完工→能耗→架次”；而我们的主方案在零软延误之后优先压缩**加权交付时刻**，ALNS 节能阶段另设加权交付不高于主方案 103% 的算法预算。这些是我们公开声明的求解策略，不是题目额外硬限制。截图未展示加权交付时刻，双方目前无法在完整字典序目标上直接比较。下一轮应把“硬截止满足、软延误为零”先固定，再分别求最短完工、最低能耗和最少架次的非支配方案，而不是让自设的 3% 门槛挡住可能更省架次的解。

## 小型路线池实验：现有搜索的结构性空白

`q2_route_pool_probe.py` 从六套原方案及两套新方案提取经过独立运输核验的路线，按“箱号集合＋服务区访问顺序”去重，得到 **52 条不同路线**。在这些既有路线中建立“每箱恰好被选一条路线覆盖”的集合划分模型，最少需 **21 条路线**。这是**当前有限路线池**的精确覆盖数，不是原第二问的全局架次下界；因此，截图若确有 20 架次同口径可行方案，至少包含我们路线池没有的新路线。

集合划分不考虑无人机和电池占用。对前 10 套 21/22 架次覆盖做后续排程尝试，有些连硬时限都无法用现有贪心调度排出；能排出的方案加权软延误仍为正。在 1 秒保守离散时间的 CP-SAT 零软延误检查中，抽查的两套 21 架次、两套 22 架次覆盖被判不可行。**这不能证明所有 21/22 架次方案都不可能**，也不能证明截图错了；它表明“只从现有路线池做最小架次集合覆盖”会遗漏时间与共享资源约束。实验明细见 `results_q2_refined/q2_route_pool_probe.json`。

## 有依据的改进顺序

1. **先统一验收口径。** 若能拿到截图完整方案，读取 80 箱逐箱交付、每架次箱组/访问顺序、A/B/C 实体机和电池编号、起飞/返航/充满时刻，以 `verify_q2` 逐项回放。若只有汇总值，标记为“未核验外部候选”。
2. **扩大路线池。** 从当前 52 条出发，系统生成新的跨服务区拼接、同区分箱和箱组重排候选。逐候选先检查质量、体积、DEM 能耗与返航 SOC，再计算“最早起飞时能否赶上各箱期望/硬截止”，筛掉显然不可能实现零延误的路线。当前的随机 ALNS 对候选只做两次贪心排程，容易误弃好路线；应对短名单调用精确资源排程器。
3. **联合覆盖与资源排程。** 路线池主问题保证 80 箱恰好覆盖一次；排程子问题给出实体机、电池和时刻的可行证书。把不可排程组合的冲突回传，继续寻找 20—24 架次区域的可行路线组合。最低覆盖数不能单独当成第二问解。
4. **输出时间—能耗—架次前沿。** 在零硬违反、零软延误条件下，分别限制完工上限或架次上限求最低能耗，并记录加权交付时刻、硬时限最小余量和 A/B/C 使用结构。与截图的快速 24/25 架次和节能 20 架次方案分别同口径比较。

目前最可能拉开差距的是**路线生成和组批**，不是在固定的 25/28 架次上继续微调起飞顺序。若以后使用额外无人机或放松题目给定截止时间，必须明确标为反事实敏感性试验，不能混作正式第二问结果。
