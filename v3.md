# v3 四问审查与改进方案

编写日期：2026 年 9 月 23 日。

本文汇总对 D 题现有 v2 程序的审查结果，逐问说明不足、改进原因、实施方式和验收标准，作为后续修改代码和撰写论文的依据。当前项目已经有可复算的四问阶段方案，主要缺口集中在核验完整性、连续通信论证、搜索范围、多目标评价和最终提交物。

**版本状态：本文是一份改进设计文档。文中的算法调整、核验增强和新增输出均为待实施事项，不代表当前代码已经升级为 v3。** 本次整理新增本文及 README 入口；已复算的数字仍属于 v2。技术依据为项目中的 `q1.py`、`q2.py`、`q3.py`、`coordination.py`、`q4.py`、`model.py`、`verify.py`、`run.py` 和原始题目。

证据分为三类：已复算或通过扰动实验确认的事实；从代码可以直接确认的实现限制与核验缺项；尚需实验判断影响大小的改进建议。搜索限制不自动等于当前解不合法，参数敏感也不自动等于题目给定参数下无解。本文覆盖前述讨论及本轮代码复核发现的事项，不将文档审查等同于已经证明不存在其他缺陷。

## 1 已验证的基础与结论边界

此前审查在内存中运行了第一问、第二问、固定运输方案的第三问基线，以及默认 v2 联合搜索和第四问。主要结果如下。

| 项目 | 已复算结果 | 可以支持的结论 |
| --- | --- | --- |
| 问题一 | 18 架次；59.229660 kWh；累计作业时间 32768.895797 s | 在现有物理模型及“架次、能耗、时间”的字典序目标下，逐服务区组批为精确解 |
| 问题二 | 80 箱，23 架次；67.861134 kWh；最晚返航 10307.701506 s | 得到满足现有程序所检查约束的可行运输方案，没有全局最优性证明 |
| 问题三 v2 | 23 个运输架次、8 个中继架次；加权交付时间 4900750.735168 s·权重；最晚返航 12548.727269 s；总能耗 72.957173 kWh | 相对固定运输基线改善了主要时效目标，但存在能耗与完工时间代价 |
| 问题三采样核验 | 80 箱交付；31 箱硬时限满足；31543 个逐秒通信采样点未发现断联；最小采样最佳链路余量 1.880385 dB | 支持所用模型下的离散采样可行性，不能据此证明所有连续时刻均可通信 |
| 问题四 v2 | 5 个不可拆任务块；两组穷举 15 种分区，三组穷举 25 种分区 | 对固定的 v2 任务块和既定评价顺序，可以查遍全部合法分区 |

一个确定的核验漏洞已用扰动实验验证：把第二问某条架次的无人机编号和电池编号改成不存在的 `U_FAKE`、`A_FAKE`，`verify_q2` 仍返回通过。该实验说明核验器存在漏检；原求解器从真实库存选设备，不能据此认定原方案实际使用了虚构设备。

审查时未发现项目中的最终论文和已填报结果模板，项目目录也没有保存本次内存复算的结果文件。后续需要形成与最终代码一致的完整提交包。

## 2 第一问 单点往返能力与货箱组批

### 2.1 当前已完成的内容

`q1.py` 根据质量、体积、航段能耗和返航安全余量枚举可行货箱子集，通过动态规划求解单服务区组批。三个目标按以下顺序比较：

1. 最小化架次数。
2. 架次数相同时，最小化总运输能耗。
3. 前两项相同时，最小化累计作业时间。

累计作业时间包含各架次的准备、装箱、往返飞行与交接时间，是所有架次作业时长之和，不是并行执行的最晚完工时间。

程序默认还会对 10%、15%、20%、25%、30% 的返航安全余量重新求解，保存各服务区、各机型的最大安全载荷，以及各区和全局架次数。因此第一问已经考虑了三个目标，也已经进行了安全余量敏感性计算。

### 2.2 不足 原因与改法

| 不足 | 为什么需要改进 | 具体怎么改 |
| --- | --- | --- |
| 目标优先级的解释不充分 | 字典序意味着极小的能耗优势也优先于较大的时间节省；“综合考虑”不等于三个指标同时达到各自最小值 | 保留当前字典序解作为基线，在论文明确顺序及救援组织上的理由；另外比较“架次→时间→能耗”或能耗容差下最小时间的方案 |
| 敏感性输出不完整 | 每档确实重新求了组批，但仅保留载荷和架次数，无法追踪换机型、换货箱组合及时间能耗变化 | 在 `solve_all` 中保存每档完整 `zone_plans`、总能耗、总作业时间、机型架次分布；在 `run.py` 中导出逐档组批明细和汇总 |
| 安全余量只试了五档 | 可以看总体变化，但难以定位增加架次或改变组批的临界区间 | 先用现有五档扫描，再在结果发生变化的区间加密；记录临界区间、受影响服务区和机型，避免把粗网格端点当成精确阈值 |
| 能耗假设的影响未充分量化 | 水平能耗及爬升附加能耗采用明确假设，结论受这些口径影响 | 固定题目给定参数作为主场景；另做有说明的能耗系数和爬升效率扰动，输出方案是否变化和变化原因 |

若增加时间优先或 Pareto 比较，不能只修改动态规划最后的比较顺序。`enumerate_batches` 当前会为每个货箱子集只保留能耗优先的一种机型。应按所选目标保留候选，或者保留该子集的非支配机型方案，再进入动态规划，否则时间更优的候选可能提前被删除。

### 2.3 验收标准

- 默认 20% 余量、原目标顺序下复现现有基线；若改变物理口径，单独说明差异来源。
- 每档余量都给出完整组批、45 个机型与服务区安全载荷值，以及总架次、能耗、累计作业时间。
- 每档均核对货箱恰好交付一次、质量体积合规、返航 SOC 达标。
- 最大安全载荷随返航余量增大不增加；可行时的最少架次数不减少。总能耗和作业时间不强加未经证明的单调要求。
- 对不可达或无法完成全部货箱的场景明确标注不可行，区分“空载也无法往返”和“可行载荷上限为零”。

### 2.4 修改时容易遗漏的细节

- `safe_payload` 目前用数值零同时表示空载往返不可行和安全载荷为零。建议返回可行状态、载荷上限和限制原因；本次默认数据下未发现安全载荷为零的机型与服务区组合，该项属于扩展场景处理缺口。
- `verify_q1` 当前只重放默认组批，不检查最大安全载荷表、敏感性各档和汇总指标。新增敏感性核验时应使用对应余量，不能用默认 20% 约束去拒绝本来按 10% 余量求得的方案。
- 对安全载荷上限，检查上下界、质量上限和能量余量；对能量限制形成的内部边界，检查边界附近的能耗变化，验证二分结果。
- 如果某档余量使某个服务区无解，保存该档不可行原因并继续分析其他档，不让一次异常终止全部敏感性报告。
- 为每个服务区标明主要限制来自载质量、装载体积还是能量，解释为什么安全载荷变化未必立刻引起架次数变化。

## 3 第二问 异构无人机与共享电池运输调度

### 3.1 当前已完成的内容

`q2.py` 先将各服务区的硬时限货箱组成任务，剩余货箱调用第一问组批；然后尝试任务排序，逐项分配具体机型、无人机和同型电池，最后通过合并架次与重新调度进行改进。医疗物资的期望送达时间已经按硬约束处理。

复算中，初始方案为 31 架次、89.128628 kWh、最晚返航 12136.737851 s；合并后改善为 23 架次、67.861134 kWh、10307.701506 s。这一方法已有有效基线，值得保留。

### 3.2 不足 原因与改法

| 不足 | 为什么需要改进 | 具体怎么改 |
| --- | --- | --- |
| 初始组批限制搜索范围 | 同一服务区的硬时限箱先放在一个任务中，可能错过拆分、夹带普通箱或跨区组合的更好方案 | 生成多种“货箱集合＋访问顺序＋机型”候选架次；保留现有方案的全部架次作为初始可行候选 |
| 设备分配主要依靠逐任务贪心 | 当前任务看起来最好的分配，可能占用后续紧急任务需要的无人机或电池 | 对选中路线统筹安排开始时刻、实体无人机和电池；发现冲突后反馈修改路线组合 |
| 不同阶段目标口径不完全一致 | 排序方案按加权交付时间优先；合并阶段优先节能，并容许相对初始基线的时效和完工时间恶化 | 集中定义统一的目标比较函数，明确采用字典序、加权目标还是容差约束；初始构造、邻域接受和最终报告使用同一套政策 |
| 加权软延误只计算而未作为首要选解指标 | 加权交付时刻会奖励所有提前送达，加权延误则直接衡量超过期望时间，两者可能偏好不同方案 | 两个指标都保留，主方案明确选择其中一种或规定优先级；使用对照结果解释选择，而非静默替换目标 |
| 搜索与质量证据有限 | 当前主要采用同类别重排和合并，不能据此判断离更优方案还有多远 | 引入拆分、整箱移动、交换、访问顺序变化和机型调整；采用一套主要搜索框架，并按相同运行时间与原基线比较 |

### 3.3 建议实施流程

1. **候选路线预计算。** 对每条候选保存货箱编号、访问顺序、机型、载荷、能耗、返航 SOC、完整时长、相对交付时刻及返航后充电时间。按硬时限计算最晚允许开始时刻。
2. **路线组合选择。** 使用以路线为变量的整数规划，要求每个货箱恰好被选中一次，并加入必要的机型和时间资源约束。候选规模过大时动态补充路线，不预先枚举全部组合。
3. **资源时序安排。** 对路线安排实体无人机和共享电池，检查从开始到返航、从开始到充满电的占用区间；不能把电池返航时刻直接当成再次可用时刻。
4. **调度反馈。** 单条路线可行不代表整组选中路线可调度。若资源冲突或硬时限不可满足，返回调整路线选择、组批或机型；不能只不断推迟起飞。
5. **邻域改进。** 可以采用 ALNS，按搜索效果调整移箱、拆分、合并等操作的使用频率；必要时借用模拟退火接受规则。无需把遗传算法、模拟退火和多个搜索框架全部叠加。

接口上可以继续复用 `model.py` 的航线计算；将 `q2.py` 拆成候选生成、组合选择、资源调度和搜索控制四部分。机型一旦改变，时间、能耗和充电时间必须重新计算。

候选架次记为 r，开始时刻为 s_r，货箱 b 在该架次内的相对交付时刻为 τ_br，则交付时刻为 s_r＋τ_br。有硬截止 d_b 时，候选的最晚开始时刻为所有硬时限箱的 min(d_b－τ_br)；候选组合还须通过实体资源调度。选取变量 x_r 需满足每个货箱对应的 sum(a_br x_r)＝1，其中 a_br 表示该候选是否携带该货箱。该表达给出了路线选择与时序安排的连接方式，不能把路线的时间成本当作与 s_r 无关的固定值。

### 3.4 验收标准

- 与现有方案比较加权交付、加权软延误、最晚返航、能耗、架次数和运行时间；目标政策变化时单独列示。
- 从原始库存核对无人机、电池编号存在性及机型匹配，逐箱核对交付和硬时限。
- 在相同计算预算下做多次搜索，报告最好值、典型值和可行率；保留原可行解，避免搜索失败导致无结果。
- 小规模实例可用完整整数规划作对照。有限候选池内的最优解不等于原问题全局最优；未经完整定价或其他证明，候选池松弛值也不能直接声称是原问题的全局下界。

### 3.5 修改时容易遗漏的细节

- `_schedule_order` 使用的是“加权送达时间＋100×能耗”的混合评分。能耗项会影响非平局情况，不能仅按代码注释将其称为平局时节能。应将系数和单位含义显式配置，或改为与主目标一致的比较方式。
- `improve_merges` 的 5% 时效容差和 10% 完工容差相对于最初基线，不是相对于上一轮方案。应说明容差参照物并做政策对照；默认实例的合并结果三个指标均改善，不属于已发生的性能退化。
- 搜索当前只在同截止类别中随机重排，且合并时主要拼接两条访问序列，未全面搜索内部插入位置和访问排列。扩展邻域时也要保留硬截止约束，不能通过放宽医疗或首批时限获得表面改进。
- 同一服务区硬时限箱全部塞进一个初始任务，如果该任务不可行，不代表拆成多个架次也不可行。增加拆分修复后再判断搜索失败，并区分“该启发式未找到解”与“已证明无解”。
- 不能仅凭 80 箱和 15 个服务区认定整体整数规划必然不可算。分解方案更便于控制规模，但应通过建模与限时实验判断；无需承诺新框架必然更快。
- 加权交付指标为 sum(w_b T_b)；加权软延误为软时限货箱的 sum(w_b max(0, T_b－d_b))。报告时解释为何选取某一指标，避免用改善前者的百分比代称后者的改善。

## 4 第三问 运输与通信中继联合调度

### 4.1 当前已完成的内容

当前 v2 已经包含联合反馈：`q3.py` 检测直连缺口、筛选中继位置并安排设备，`coordination.py` 尝试波次插入、整箱移位、拆并架次以及访问和执行顺序调整，每个候选重新进行运输和中继求解。

默认试算 18 个候选，其中 13 个通过完整第三问求解。最终采用提前插入 T021 架次的改动，使加权交付时间相对固定运输基线降低 3.39%；最晚返航增加 103.36 s，总能耗增加约 0.131 kWh。当前路线邻域候选未产生进一步改进，应如实报告这一结果。

### 4.2 不足 原因与改法

| 不足 | 为什么需要改进 | 具体怎么改 |
| --- | --- | --- |
| 通信保证仍依靠离散采样 | 缺口检测、最终检查分别采用不同时间步长，且遮挡判断也按空间点采样，短时断联可能漏检 | 用逐栅格视线判断替代单纯空间采样，并对时间区间构造保守可用性界限；无法证明的区间继续细分或明确标记未证实 |
| 候选位置和高度较粗 | 当前位置网格约为 0.005°，只试 200 m 和 300 m 离地高度，可能错过更好的位置与高度 | 保留粗网格初筛，再在有希望的位置局部细化；满足 DEM 覆盖、悬停高度、双向回传链路和能源约束 |
| 覆盖筛选以整个架次为单位 | 要求某个候选位置覆盖一架次的全部采样缺口，可能排除由不同中继分阶段接力的可行方案 | 将覆盖对象细化为通信缺口区间，允许各区间由不同中继任务承担，并核对切换时无保障空档 |
| 波次结构和起飞时间调整较受限 | 按截止时间形成波次，可能使运输机等待中继，或让中继长时间悬停 | 生成带有位置、高度、建链和服务区间的中继任务，联合考虑飞行、周转、组件充电与运输开始时刻 |
| 联合搜索预算偏小 | 已建立反馈框架，但默认只有两轮路线搜索，每轮最多三次完整中继试算，不能据此说明搜索充分 | 缓存固定航段与候选链路结果，增加多起点或时间预算，并记录收敛过程与各操作改进贡献 |
| 第三问隐含了第四问可分三组的限制 | `q3.solve` 会排除不可拆任务块少于三个的候选，可能牺牲第三问自身目标 | 将“保证后续可分三组”设为显式选项；对照无该附加条件与带该条件的结果，并在论文报告代价 |
| 最小采样余量偏紧 | 已复算余量约 1.88 dB，统一增加 2 dB 系统损耗将使原方案至少一个已检查点失联 | 对链路损耗、门限等做参数扰动；分别报告原方案抗扰能力和扰动后允许重优化的结果，避免混淆 |

### 4.3 建议实施流程

1. 以第二问为初始解，识别每条运输轨迹上的直连不可用区间，保存相对时间和绝对时间。
2. 在 DEM 内生成可行悬停候选，将接入和回传两段双向链路同时可用作为覆盖条件。
3. 生成完整中继架次候选，包括准备、往返、建链、服务区间、能耗、返航 SOC、设备周转和能源组件充电。
4. 选择能够覆盖缺口的中继任务并安排资源。空间覆盖可行仍须通过时序和能量检查；覆盖目标不能只最小化中继位置个数。
5. 将中继等待、能耗和难以保障的区间反馈给运输搜索，尝试移箱、拆并、改变访问顺序、机型和开始时刻，重新计算联合方案。
6. 对最终方案给出连续区间核验结果及参数扰动报告，再交给第四问。

题目规定运输节点间为水平直线航段，巡航海拔由沿线最高地形加 50 m 确定。此处运输路线调整应限定为组批、节点访问顺序和架次组织的改变；不得为了改善通信擅自添加绕山航点或自由修改运输巡航高度。

联合目标应明确同时计入运输与中继任务。当前最终比较函数的架次项只使用中继架次数；如果论文采用“两类总架次数”，应补上运输架次数，或者明确分别比较两类架次的优先顺序。

连续性论证应限定在题目给定的 DEM 与传播模型内。建议按航段阶段、视线穿越栅格和中继服务事件划分区间，给出区间内保守链路余量；遇到可能发生遮挡状态切换的区间，应使用有保证的界限或进一步分割。仅把采样步长从 1 s 改成 0.1 s，仍不能自动获得数学上的连续保证。

### 4.4 验收标准

- 对爬升、巡航、下降和交接全过程，逐区间核对保障覆盖；每个中继服务区间同时满足接入与回传链路要求。
- 对通信表逐条检查运输架次、阶段、起止时间、中继编号和实际链路；无漏段、矛盾重叠或已离场中继继续服务。
- 核对中继到达及建链完成时刻、服务结束与返航关系、SOC、周转和组件充电时间。
- 区分“采样通过”“连续区间已证明”“尚未证明”，不把未证明直接视为可行或已断联。
- 在统一目标和相同时间预算下，与固定运输基线和现有 v2 比较，并公开时效、完工时间与能耗的交换关系。

### 4.5 第二轮代码复核补充事项

下列行为均可从实现直接确认；除另有说明，尚未通过替代算法实测其对本次最优目标值的影响。

| 位置 | 当前限制或缺项 | 影响与具体改法 |
| --- | --- | --- |
| `q3.candidate_sites` | 搜索范围是服务区包围框外扩 0.01°后与 DEM 的交集，并非整个 DEM | 可能忽略范围外的优良中继点。先解释缩小范围的依据，必要时扩展全域粗筛，再局部细化 |
| `q3.site_choices` | 只要存在单位置覆盖方案，就直接返回单位置列表，不再考察双位置组合 | 空间上能单点覆盖，不代表单架中继能按时到达并完成全时段服务。将单点和双点候选一起评价，至少在单点调度失败时尝试双点 |
| `q3.split_groups_for_coverage` | 主要在空间覆盖失败时拆波次 | 空间可覆盖但悬停能量不足或资源冲突时，拆分时间波次也可能可行。增加调度失败后的分波、换位或接力修复 |
| `q3._trial_wave` | 在一次中继求解中固定输入方案的运输机和电池编号，主要调整开始时刻 | 外层虽可改变运输分配，内层仍可能因不合适的固定编号而错失可行调度。增加同机型资源重分配或与运输调度器交替求解 |
| `q3._trial_wave` | 一个中继任务从最早缺口一直服务到最晚缺口，并前后固定预留 90 s | 中间可能存在较长的无需求时段；固定缓冲也可能增加能源消耗。保留安全保障的同时比较拆分服务任务，并用有依据的区间边界和缓冲参数替代无解释常数 |
| `q3.solve` | 束搜索宽度固定为 6，每波候选位置组合截断为 45；中间排序使用 weighted＋400×ready＋20×energy | 这是启发式剪枝，不等于最终字典序目标。参数化预算和系数，解释中间评分用途，做宽度及预算对照，避免声称剪枝保持最优性 |
| `coordination.solve` | 波次提前试算与路线邻域试算分开；后者调用中继求解时未继续传入提前方案 | 可能漏掉“路线改变＋波次提前”才能产生的组合收益。候选状态同时记录运输任务和波次安排，每轮对可匹配的新架次重新搜索提前方案 |
| `coordination._promotion_options` | 只取排序后的前若干提前候选，默认每次只提前一个架次 | 候选截断存在次序偏好。增加分层或轮转选择，并在预算允许时尝试多架次共同调整 |
| `coordination.solve` | 操作选择按权重、名称排序后截断；操作名顺序会影响有限预算分配 | 某些操作可能长期得不到试算。增加轮转或探索配额，记录每种操作的生成、完整评估、可行与接受次数 |
| `q3._verify_communication` | 发现采样模式改变时，把切换时刻记为该采样时刻 | 不能保证前一完整区间一直符合旧模式。改为定位实际事件边界，或输出边界不确定区间并保守核验 |
| `q3` 的覆盖字段 | `covered_transport` 来自计划分配，最终通信表又会从所有在场中继中选可用链路 | 两份保障关系可能不一致。分别命名为计划覆盖和实际采用关系，最终资源分区以核验后的实际关系为准，并同步导出 |
| `verify.verify_q3` | 最小最佳链路余量取直连或中继接入余量，回传只另外检查是否可用 | 应报告端到端余量：中继路径取接入与回传余量的较小值，再与直连比较。当前候选回传门槛为 2 dB，但指标定义仍应完整 |
| `run.main` 与 `q3.solve` | 求解阶段按 2 s 检查，最终 1 s 复核失败时会中断流程，没有自动返回选择其他候选 | 在最终接受前运行足够严格的统一核验；候选失败要反馈搜索或修复，不能降低核验强度换取运行成功 |

“候选链路预留 2 dB”“缺口前后预留 90 s”和“按 1 s 检查”分别是余量门槛、时间缓冲和采样密度，不能相互替代，也不能合起来自动构成连续通信证明。说明文档应逐项报告这些参数及其适用范围。

题目未给出同一中继的并发容量上限，当前允许同时保障多架运输机，这属于显式假设；不要在主模型中擅自添加容量限制。若研究此假设的影响，应另列扩展场景。

## 5 第四问 任务分区与资源配置

### 5.1 当前已完成的内容

`q4.py` 已经依据同一运输架次涉及的服务区，以及同一中继架次实际保障的运输任务建立关联，通过传递关系形成不可拆任务块。仅仅由同一实体无人机执行过不同架次，不会直接使这些服务区绑定。

当前 v2 的 5 个任务块足以直接穷举两组的 15 种、三组的 25 种分区。因此应保留精确枚举，无需为该规模改用近似聚类或遗传算法。

程序同时提供两种资源口径：保留原设备编号时按各组不同编号数量核算；允许组内重新分配同类设备时，按包含充电或周转的最大同时占用数量核算。还输出最小缺口、最佳均衡和额外资源受限的折中方案。

### 5.2 不足 原因与改法

| 不足 | 为什么需要改进 | 具体怎么改 |
| --- | --- | --- |
| 任务块依赖第三问通信记录的正确性 | 通信表若漏写或错写中继关系，会改变不可拆任务块，进一步改变分区及缺口 | 第四问只接收已通过通信表核验的第三问结果；保存每个任务块的运输及中继关联依据 |
| 两种资源口径的地位不够清楚 | 保留编号可能比固定时序下重新分配同类设备需要更多资源，不能混称为最低需求 | 分别报告两套需求及缺口，并明确对“保持第三问任务安排”的解释；不要合并成一个未经说明的结果 |
| 目前按严格编号口径选优 | 虽计算了重新分配编号后的需求，但最优分区主要由严格编号缺口决定 | 在两种口径下分别重新评价全部分区，列出各自最优方案；不能假定同一分区同时对两种口径最优 |
| 不同资源缺口直接相加 | 一架无人机和一组电池各计一个单位，不能直接代表相同成本或部署难度 | 先保留各类资源缺口向量；有依据的成本或权重另作为情景参数，不虚构采购价格 |
| 最小缺口方案可能严重失衡 | 当前均衡性排在缺口和分区额外资源之后，可能产生一组承担大部分任务的方案 | 并列展示最少资源、最均衡、满足指定均衡阈值下最少资源三类方案，并给出不被其他方案全面优于的候选 |
| 工作量只用累计任务时长概括 | 相同总时长可能对应不同峰值压力、货物质量和服务人口 | 保留累计时间和变异系数，同时列出箱数、质量、任务数、峰值设备占用；人口可作为解释指标，不直接替代物资需求 |

### 5.3 建议实施方式

继续保留“关联关系→连通任务块→穷举分区→逐组资源核算”的主流程。对每种资源，明确占用区间：运输机占用到返航，电池占用到充满；中继机占用到返航并完成周转，能源组件占用到充满。

在同类资源可互换、任务时间固定、全部从 O01 开始并返回的当前假设下，最大同时占用数可用于求最少设备数量。为使结果可执行，进一步用区间分配算法生成各组具体设备编号，并再次检查所有占用区间。

分开报告三个概念：分区总需求相对于统一执行需求的增加量、相对于库存的缺口，以及选定配置相对于最低需求的富余量。三者含义不同，不能全部写作“资源冗余”。

当前严格编号口径下，两组方案最少新增 1 架 C 型运输机和 1 组 C 型电池；三组最少新增 2 架 A 型、1 架 C 型运输机，以及 1 组 A 型、1 组 C 型电池。这些数值只属于当前 v2 方案、关联关系和该口径。第三问改变后必须重新计算。

### 5.4 验收标准

- 15 个服务区恰好分配一次，各组非空，不拆分经核验的运输或中继关联任务块。
- 对两组和三组都查遍合法分区，分别按两套资源口径评价；第三问任务块变化时，不再沿用旧的分区数量和结果。
- 独立核对库存、各组资源需求、总需求、缺口及均衡指标；主方案和所有备选方案均须通过核验。
- 允许重新分配设备的方案应输出可执行的编号分配，证明峰值资源数量足够完成固定任务。
- 不改变第三问已固定的组批、访问顺序、时间安排和通信关系；需要改变时应返回第三问形成新版本。

### 5.5 修改时容易遗漏的细节

- `run.export` 的第四问 CSV 目前写入 `strict_ids`，汇总 JSON 也主要展示严格口径；重新编号口径的详细值主要保留在完整 `q4.json`。新增 `resource_policy` 字段并输出两套可比较表，避免代码算了两套而论文或模板只看见一套。
- 当前“资源缺口＋最多两个额外资源”的折中阈值是固定的简化选择。应参数化，并展示改变阈值后的均衡代价；不同资源仍须分类型列示。
- 除箱数与总时长外，保存每组任务明细和关联来源。若某个已安排的中继架次最终未被通信表使用，应明确其处理政策；固定第三问方案后，不能无说明地在第四问删去任务以降低资源数。
- 第四问的不拆任务块来自固定运输与中继架次关系，不是单纯的地理距离聚类。当前空间离散度只能作为辅助评价；题目没有要求时，不应额外强制每组地理连通。
- 最大同时占用数应按机型分别计算，结束事件与下一次开始事件重合时采用统一边界约定；不得跨机型互换电池。对严格编号口径，同一原编号出现在不同组时，应在独立配置中体现所需复制设备。
- 若固定第三问关系后不足三个任务块，三组划分不可行。应明确报告原因，或返回第三问形成标识清楚的新方案；不能在第四问悄悄拆中继关系。第三问是否预先保留可分性，见第 4.2 节的显式选项。

## 6 跨四问的核验与交付改进

### 6.1 优先修补核验器

`verify.py` 目前与求解器复用了 `Scenario.route`、`relay_sortie`、`link_margin`，第四问又复用了 `resource_need`。这些复算能够发现记录不一致，但无法独立发现共享公式本身的系统性错误，应区分“同模型重放”与“独立实现核对”。

具体改动如下：

- 检查所有实体无人机、电池、中继机、能源组件编号存在，类型匹配，数量不超过所选库存或明确新增配置。
- 检查架次编号唯一、开始时刻非负、时间与能量为有限数值；逐箱交付记录中的架次和服务区须与实际运输任务一致。
- 从重算轨迹核对保存的阶段时间和位置；不能仅校验载荷、能耗后继续信任未经核对的 `route.phases`。
- 对中继海拔与 DEM 加离地高度、建链完成时刻、返航 SOC、能源组件再次可用时刻逐项重算。
- 完整核对通信表的时间覆盖和保障关系，而不是只确认 `mode` 属于 `direct`、`relay`。
- 对第四问重新核对库存缺口和全部备选分区，避免只调用同一资源统计函数便宣布独立验证。
- 增加针对漏检的故障注入检查：虚构编号、错配机型、重复或漏交货箱、提前使用未充满电池、错误中继编号、通信表漏段等都应被明确拒绝。
- 对关键物理公式准备可手算的小例子；对地形视线和资源区间计数采用独立实现交叉核对。

### 6.2 数据与物理口径

- `Scenario` 当前大量按固定行位置读取 Excel，应增加表头、ID 唯一性、类型、单位、库存及逐箱需求一致性检查，避免数据格式变化后静默误读。
- `DEM.peak` 当前会过滤 NoData 后取剩余像元最大值。对于穿过未知地形的航段，应报错或按明确保守政策处理，不能把未知地形直接当作已满足净空。该项是代码层面的潜在风险，目前未证明现有路线实际受到影响。
- 固定题目给定规则作为主场景；区分题目参数、为补足公式所作假设、以及用于敏感性分析的扰动参数。
- 保存输入文件摘要、代码版本、依赖版本、参数、随机种子、运行时间和目标政策，保证论文与结果文件可对应复现。

### 6.3 输出与论文

- 用独立结果目录保存 v2 基线和后续 v3 结果；修正核验或物理口径导致数字变化时，说明原因。
- 在 `run.py` 增加各问改进所需汇总和明细输出；确定第三问最终方案后重新生成第四问。
- 将最终结果填入提交模板，并核对箱数、编号、时间单位、能耗单位、SOC 百分数和资源口径。
- 论文给出目标函数、约束、算法步骤、对照实验、敏感性分析与结论适用范围；问题二、三没有证明时使用“搜索得到的较优可行方案”。
- 图表与正文统一使用同一版本的数据，展示路线、调度时序、通信保障及分区权衡。

### 6.4 统一的结果接口与实验记录

以下文件名为后续实现建议，本次仅写入设计文档，不代表对应文件已经生成。

| 建议输出 | 必须包含的内容 | 对应验收目的 |
| --- | --- | --- |
| `q1_reserve_summary.csv` | 余量、可行状态、总架次、总能耗、累计时间、分机型架次数 | 完整展示安全余量影响 |
| `q1_reserve_batches.csv` | 余量、服务区、机型、货箱、载荷、体积、能耗、时间、SOC | 解释组批和机型切换 |
| `q2_search_history.csv` | 时间预算、种子、目标政策、候选状态、主要指标、核验状态 | 与现有启发式作公平比较 |
| `q3_interval_validation.json` | 阶段与时间区间、保障方式、接入与回传界限、证明状态和失败原因 | 追溯连续通信结论 |
| `q3_robustness.csv` | 扰动参数、原方案表现、重优化表现、时限与链路风险 | 区分固定方案鲁棒性和再规划能力 |
| `q4_partition_resources.csv` 扩展 | 资源口径、分区方案、逐类需求与缺口、工作量和关联任务 | 比较两种资源政策及均衡取舍 |
| `validation.json` 扩展 | 数据检查、资源编号、箱级核验、通信表、区间证书、分区及备选核验 | 避免用一个通过标记掩盖未检查项目 |
| `run_manifest.json` | 输入摘要、代码与依赖版本、参数、种子、耗时、输出清单、结果版本 | 支持论文和模板复现 |

对比实验至少保留现有 v2、只修正核验的版本和新增优化算法后的版本。若模型或核验标准改变，应先重评旧方案，再比较新旧结果，避免将更宽松的约束当作优化收益。缓存键必须包含机型、货箱与访问顺序、物理参数及通信参数版本，防止扰动分析误用旧结果。

## 7 建议实施顺序与文件分工

| 优先级 | 工作 | 主要文件 | 完成依据 |
| --- | --- | --- | --- |
| P0 | 补齐编号、资源、轨迹与通信表核验 | `verify.py`、`model.py` | 已知故障注入被拒绝，原方案接受或明确报告此前漏检的真实问题 |
| P0 | 加强地形遮挡与连续通信论证 | `model.py`、`q3.py`、`verify.py` | 每个必要区间有可用性结论；不能证明的区间明确列出 |
| P1 | 补齐第一问敏感性结果及目标对照 | `q1.py`、`run.py`、`figures.py` | 逐档完整组批及架次、能耗、时间对比 |
| P1 | 统一第二、三问目标，完善路线选择与调度反馈 | `q2.py`、`coordination.py`、`q3.py` | 同预算基线对照、完整资源可行性与搜索记录 |
| P1 | 改进中继区间覆盖并测试参数扰动 | `q3.py`、`coordination.py`、`model.py` | 时效与资源权衡、原方案及重优化方案的抗扰结果 |
| P1 | 分别优化第四问两种资源口径并展示均衡权衡 | `q4.py`、`verify.py`、`run.py` | 全部合法分区核验、两种资源配置及缺口表 |
| P2 | 整理最终模板、图表、论文和复现记录 | `run.py`、`figures.py`、项目说明及结果文件 | 提交物齐全且各处数字一致 |

执行时先建立可靠核验，再扩大搜索。第一问可在核验修补后独立完善；第四问应在第三问最终方案确定后定稿。保留现有可复算基线，以实验判断新方法是否在统一标准下改善，而非预先把更复杂的算法等同于更好的结果。

## 8 方法参考

- Ropke 与 Pisinger：自适应大邻域搜索及多个破坏、修复操作的自适应选择。可作为第二、三问搜索设计的参考，不能直接证明本题实现的性能。https://orbit.dtu.dk/files/3154899/An%20adaptive%20large%20neighborhood%20search%20heuristic%20for%20the%20pickup%20and%20delivery%20problem%20with%20time%20windows_TechRep_ropke_pisinger.pdf
- SCIP 车辆路径示例：路线变量和列生成实现参考。https://scipopt.org/doc/html/VRP_MAIN.php
- Rottondi 等：灾后无人机多任务联合规划及启发式求解参考。https://arxiv.org/abs/2010.10939

这些资料提供方法背景。实际计算、约束口径和验收以本题原始材料及可核查的代码结果为准。

## 9 讨论内容逐项核对清单

下表用于检查文档覆盖范围；实现完成情况仍以代码变更和验收结果为准。

| 核对项 | 文档位置 | 状态与说明 |
| --- | --- | --- |
| 第一问已经考虑三目标，采用架次优先 | 2.1、2.2 | 已实现，需充分解释优先顺序 |
| 累计作业时间与并行完工时间的区别 | 2.1 | 已说明 |
| 10% 至 30% 五档返航余量已经重新求解 | 2.1 | 已实现，不误写为未开展 |
| 敏感性缺少完整组批、机型、能耗和时间 | 2.2、2.4 | 待补输出和核验 |
| 更改目标时不能保留原有单一机型筛选 | 2.2 | 待随目标扩展一起调整 |
| 第二问现有初始解与合并改进有实际效果 | 3.1 | 已复算，保留为基线 |
| 路线可行不保证资源可调度 | 3.3 | 已给出调度反馈改法 |
| 候选必须包含货箱和机型，不能只有节点路径 | 3.3 | 已给出数据接口与连接公式 |
| 混合评分、字典序、5% 和 10% 容差应统一解释 | 3.2、3.5 | 已定位到具体函数 |
| 加权交付与加权延误不能混用 | 3.2、3.5 | 已给出指标定义 |
| ALNS、整数规划与整体模型的适用边界 | 3.3、3.4、3.5 | 有限候选最优不作全局承诺 |
| 第三问 v2 已有联合搜索 | 4.1 | 已明确，不误写为机械添加中继 |
| 3.39% 改善伴随完工时间和能耗代价 | 4.1 | 已复算，保留完整权衡 |
| 两段双向链路必须在同一时刻可用 | 4.3、4.4 | 列为最终验收条件 |
| 时间采样与空间采样不能证明连续通信 | 4.2、4.3、4.5 | 待补保守区间核验 |
| 位置范围、网格、高度和单点优先限制 | 4.2、4.5 | 已给出扩展及失败修复方式 |
| 波次与路线调整需要真正组合搜索 | 4.5 | 已指出提前方案未叠加的限制 |
| 运输航段必须遵守节点间直线和规定海拔 | 4.3 | 已纠正自由绕行建议 |
| 第三问预设三组可分性属于附加限制 | 4.2、5.5 | 待显式配置并比较代价 |
| 小余量、参数扰动和中继并发假设 | 4.2、4.5 | 已区分主场景与扩展分析 |
| 第四问同时保留运输和中继关联 | 5.1、5.5 | 已实现，依赖通信表正确 |
| 小规模任务块应保留穷举 | 5.1 | 已说明无需近似聚类替代 |
| 两种资源口径分别选优并完整导出 | 5.2、5.5 | 待改评价与 CSV 输出 |
| 工作量均衡、资源代价与冗余概念 | 5.2、5.3、5.5 | 已给出多方案比较方法 |
| 虚构设备编号能通过现有复核器 | 1、6.1 | 已用故障注入证实，优先修补 |
| 复核复用了模型，存在共同错误漏检 | 6.1 | 已区分重放与独立实现 |
| 轨迹、通信表、中继 SOC、备选分区核验不全 | 4.4、4.5、5.4、6.1 | 已列具体检查项 |
| 输入行号、NoData、能耗假设及版本记录 | 6.2、6.4 | 已区分潜在风险和当前事实 |
| 最终论文、结果模板、图件及版本一致性 | 6.3、6.4 | 待形成完整提交物 |
| 各问实施顺序和文件分工 | 7 | 已给出优先级及完成依据 |
