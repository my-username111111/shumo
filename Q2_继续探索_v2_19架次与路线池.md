# 第二问继续探索 v2：箱级重分配得到 20 与 19 架次

## 结论与选用

上一轮的 21 架次解无法再把两条**完整路线**直接合并。原因之一是 S001 的三条路线载重分别为 68、20、66 kg：68+20 和 66+20 都超过 C 型 80 kg 上限，但把 20 kg 的三个箱子分别分给两条 C 路线，可以变成 74 和 80 kg 两架次。新方法把货箱从路线中取出重新组批，再用题目 DEM 物理模型和实体机/共享电池 CP-SAT 排程核验，得到 20 架次。继续对 S006/S007 的三条路线做 B+C 两架次重新组批，得到多套 19 架次方案。

| 方案 | 架次 | A/B/C 架次 | 加权软延误 | 加权交付时刻（优先级·秒） | 最晚返航 | 能耗 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 28 架次时效主方案 | 28 | 14/8/6 | 0 | 2,662,962.480 | 7,350.407 s | 77.059459 kWh |
| 23 架次均衡方案 | 23 | 8/8/7 | 0 | 2,666,792.761 | 7,663.737 s | 69.946565 kWh |
| 21 架次旧低架次方案 | 21 | 6/8/7 | 0 | 2,948,652.297 | 8,225.342 s | 66.890893 kWh |
| **20 架次重分箱方案** | **20** | **5/8/7** | **0** | **2,967,133.784** | **8,335.749 s** | **66.274584 kWh** |
| **19 架次交付优先** | **19** | **5/7/7** | **0** | **2,958,556.179** | **8,466.170 s** | **65.606599 kWh** |
| 19 架次完工优先 | 19 | 5/7/7 | 0 | 3,038,621.666 | 8,291.342 s | 65.606599 kWh |
| 19 架次能耗优先 | 19 | 4/8/7 | 0 | 3,060,390.622 | 8,498.011 s | **65.217504 kWh** |

以 20 架次方案为基线，19 架次交付优先方案的加权交付时刻再减少 8,577.606 优先级·秒，能耗减少 0.667985 kWh，架次少 1，但最晚返航晚 130.421 秒。19 架次完工优先方案则比 20 架次提前 44.407 秒返航，能耗也更低，不过加权交付时刻更晚。论文应同时呈现这些指标，不能把“19 架次”单独写成全面最优。

所有新方案都由 `verify_q2` 对 80 箱、31 个硬截止箱、载重/体积、DEM 航时与能耗、返航 SOC、实体机与同型电池充电占用做独立回放。20 架次方案最小硬时限余量为 292.89 秒；19 架次交付优先与完工优先方案分别为 118.63 和 52.63 秒，最低返航 SOC 均为 22.79%。后两种方案虽满足题目要求，余量较小，应在论文中给出敏感性或鲁棒性讨论。

## 方法与有限搜索范围

- `q2_ejection_search.py`：一条架次的箱子分送到两条既有架次，即 3→2 箱级重分配。S001 的候选经完整排程后生成 20 架次可行解；随后对同一箱组最小化加权交付时刻，固定箱组的离散 CP-SAT 求解状态为 `OPTIMAL`。
- `q2_three_to_two_search.py`：对指定三条路线抽出全部箱子，枚举 B+C 两条新路线的箱子划分和服务区访问顺序。S006/S007 共得到 102 个通过物理和单航次时限初筛的候选。每个候选还须重新排八架实体运输机与同型共享电池；最终 19 架次完整方案再次通过连续时间核验。`q2_frontier_refine.py` 在固定箱组下分别压缩加权交付、最晚返航或能耗。
- `q2_route_pool_probe.py`：把已核验方案及新搜索产生的路线纳入 exact-cover 路线池，再做资源排程。61 条已核验路线组成的有限池中，全部 68 个 17/18 架次 exact-cover 组合经 CP-SAT 判为 `INFEASIBLE`。加入重分箱产生的候选路线后，路线池扩至 168 条；抽取的 50 个 17 架次、以及按两种排序各 100 个 18 架次覆盖方案，也都被 CP-SAT 判为不可行。**这些都是有限路线池结果，不是 18 架次全局不可行证明。** 更大的 4→3 箱级重组仍可探索。

方法取向与[François、Arda、Crama（2019）的多架次时间窗联合搜索](https://pubsonline.informs.org/doi/10.1287/trsc.2019.0909)一致：路线变化必须反馈到车辆排程；本题还增加了同型共享电池充电日历。[Friedrich、Elbert（2021）的路线池重组](https://arxiv.org/abs/2107.06086)启发了有限 exact-cover 试验，但其原题车辆约束不能照搬为本题的总架次数上限；实体机可以重复执行架次。以上是方法借鉴，并非复现论文数值。

## 交付文件与复现

- `results_q2_extended/comparison.csv`、`fleet_mix.csv`：12 套方案的多指标与 A/B/C 实体机、架次、占用率对照。
- `results_q2_extended/plans/`：12 套逐架次、逐箱完整 JSON；`verification.json` 为独立核验结果。
- `results_q2_extended/result_template_20_repacked.xlsx`、`result_template_19_best_delivery.xlsx`、`result_template_19_fast.xlsx`、`result_template_19_min_energy.xlsx`：填写原题 Q2 两张工作表的结果模板；同名 `sorties_*.csv`、`deliveries_*.csv` 便于队友检查。
- `results_q2_extended/search_logs/`：箱级重分配候选、求解状态和固定箱组排程报告；`search_20_inputs/` 与 `search_20_summary.json`：上一轮 20 架次局部审计的完整输入与结论；`route_pool_18_*.json`：有限路线池探查。

在仓库目录运行，原题数据沿用上一级 `D题/数据`：

```powershell
python q2_ejection_search.py --input results_q2_extended/plans/21_low_sorties.json --output results_q2_ejection
python q2_frontier_refine.py --input results_q2_ejection/best_plan.json --output results_q2_ejection/weighted_refined --objective weighted --energy-cap 66.5 --makespan-cap 8340 --seconds 60
python q2_three_to_two_search.py --input results_q2_ejection/weighted_refined/plan.json --source-sorties 6,8,11 --first-model B --second-model C --limit 102 --objective weighted --output results_q2_three_to_two_weighted
python build_q2_extended.py
python q2_audit_20_branches.py
python q2_route_pool_probe.py --extra-plan results_q2_extended/plans/21_low_sorties.json --extra-plan results_q2_extended/plans/20_repacked.json --extra-plan results_q2_extended/plans/19_best_delivery.json --extra-route-search results_q2_extended/search_logs/three_to_two_energy_search.json --min-count 18 --max-count 18 --zero-soft --candidates 100 --direct-exact --exact-seconds 3 --rank-objective energy --output results_q2_extended/route_pool_18_recheck.json
python -m unittest discover -s tests -v
```

CP-SAT 使用多线程，重新运行可能得到另一份同等可行的排程；交付 JSON 是本次实际结果。19/20 架次均只针对第二问运输，若给第三问使用，必须重新求连续通信保障。
