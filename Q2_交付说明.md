# 第二问运输调度交付说明

## 运行与文件

在本目录执行 `python run.py --only-q2`。程序读取上一级 `数据` 目录的原始附件，把第二问结果单独写入 `results_q2`。完整四问仍执行 `python run.py`。如需指定位置，使用 `--output 目录`。

主要文件：

| 文件 | 内容 |
| --- | --- |
| `q2.json` | 主方案、五套完整备选方案及搜索记录 |
| `q2_transport_sorties.csv`、`q2_box_deliveries.csv` | 主方案的架次和逐箱送达表 |
| `q2_zero_delay_efficient_*.csv`、`q2_delivery_priority_*.csv`、`q2_energy_guarded_*.csv`、`q2_energy_guarded_timely_*.csv`、`q2_alns_zero_delay_efficient_*.csv` | 五套备选方案 |
| `q2_tradeoff.csv` | 六套方案的五项指标对比 |
| `q2_deadline_audit.csv` | 六套方案逐箱期望时间、硬截止、交付时间与余量 |
| `q2_resource_timeline.csv` | 六套方案每架运输机与电池的任务占用和充电区间 |
| `q2_validation.json` | 货箱、架次、硬时限和备选方案核验数量 |
| `q2_result_template.xlsx` | 原题结果模板的两个 Q2 工作表已填写；其余四问工作表保留空白 |

## 题目约束与目标

每个货箱只能交付一次。每架次可访问一个或多个服务区，货箱不可拆分；航线按原题的节点直线、DEM 净空、载质量、体积和返航余量规则计算。医疗物资的期望送达时间及首批保障截止时间均作为硬约束。运输机和同型共享电池分别占用；电池返航后须按附件的两阶段模型充至 100% 才能再次使用。

主方案按“加权软延误、加权交付时刻、最晚返航、运输能耗、架次数”的字典序比较。加权软延误仅对没有硬截止的货箱计算 `优先系数 × max(0, 实际交付时刻－期望送达时刻)`；加权交付时刻对全部货箱计算 `优先系数 × 实际交付时刻`。硬截止与普通物资期望时间统一放在实际时间轴排序，每个候选计划再逐箱检查硬截止。另保留零软延迟下较省能耗、加权交付时刻优先、限时节能及其时序改良，以及两阶段 ALNS 零延迟节能方案。六套均是搜索得到的可行方案，不声称全局最优。

## 本次复算结果

| 方案 | 加权软延误 / s·权重 | 加权交付 / s·权重 | 最晚返航 / s | 能耗 / kWh | 架次 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 软延误优先，主方案 | 0 | 2,735,557.495 | 7,558.434 | 77.059 | 28 |
| 零延迟较省能耗 | 0 | 2,800,522.364 | 7,531.737 | 76.401 | 27 |
| 交付时刻优先 | 10,639.753 | 2,771,259.248 | 7,629.680 | 73.210 | 25 |
| 限时节能，供第三问接续 | 72,248.943 | 3,203,874.303 | 9,345.191 | 67.929 | 23 |
| 限时节能的时序改良 | 30,223.122 | 3,094,451.202 | 9,377.377 | 67.768 | 23 |
| 两阶段 ALNS 零延迟节能 | 0 | 2,809,611.621 | 7,531.737 | 74.337 | 25 |

原调度器先安排全部硬截止批次，再安排普通批次，使 10800 秒硬截止货箱排在 3600 秒期望货箱之前。此次修正统一交期排序，并让 C 型机的单服务区大批货箱也可拆分后重新调度。相对修正前主方案，软延误从 94,935.285 降为 0，最晚返航从 10,099.547 秒缩至 7,420.680 秒；代价是能耗从 75.804 增至 78.050 kWh、架次从 24 增至 28。节能方案经架次顺序搜索也把软延误从 489,989.334 降至 30,223.122，同时能耗从 67.861 降至 67.768 kWh。该时序改良版暂未被当前第三问中继搜索覆盖，因此第三问仍从原限时节能版启动。

本轮继续修复了跨路线搬箱时“末尾插入”候选会重复加入同一货箱的问题，并把路线邻域扩展为所有访问位置插入、跨路线换箱和四轮迭代搜索。固定路线集合之后，新增有界束搜索，同时选择任务顺序、可行机型、实体无人机和同型电池；64 个保留状态共展开 114,520 个资源分配候选，得到 64 个完整计划。所有搜索阶段统一按各自声明的最终目标比较，不再让中间阶段的最晚返航指标提前淘汰加权交付更好的方案。

与上一版零延迟主方案相比，新主方案仍为 28 架次、零软延误，加权交付时刻减少 130,384.426 s·权重，能耗减少 0.991 kWh，但最晚返航增加 137.754 s；这是字典序主目标下的有效改进。零延迟较省能耗方案由 27 架次、77.473 kWh、8,791.602 秒改进为 27 架次、76.401 kWh、7,531.737 秒，同时加权交付时刻也下降。节能两套方案未改变。

新增 `q2_alns.py` 实现两阶段自适应大邻域搜索。第一阶段以软延误和加权交付时刻为优先目标，500 次迭代未超过现有主方案。第二阶段固定软延误为 0，并把加权交付时刻限制在第一阶段结果的 103% 以内；三个随机种子各运行 1,200 次，在搬箱、换箱、合并、拆分、访问重排和移除重插六类算子之间按历史收益调整选择权重。三个种子共得到 2,530 个可行候选，最佳方案为 25 架次、74.336635681 kWh、7,531.736524 秒，加权交付时刻为 2,809,611.621，满足设定上限 2,817,624.220。

相对原零延迟较省能耗方案，ALNS 方案保持零软延误和相同最晚返航时间，减少 2 个架次并节省 2.064863 kWh；加权交付时刻增加 9,089.257，约 0.32%。因此它作为资源节约对照方案导出，没有替换按既定字典序选出的主方案。

核验器重算了 80 箱交付、31 箱硬时限、运输航线、载荷、体积、能耗、返航 SOC、电池充电可用时刻和实体资源区间。它还检查无人机、电池编号是否存在及机型是否匹配、架次编号是否唯一、逐箱记录的架次与服务区是否正确、保存的航段阶段是否与重算轨迹一致，以及目标汇总是否吻合。五套备选方案也各自通过同样核验。`tests/test_q2_verifier.py` 的故障注入覆盖虚构编号、错配机型、重复架次、错误交付来源、轨迹篡改和目标篡改，并增加统一交期、联合资源束搜索和短预算 ALNS 的回归检查。

## 与第三问衔接

第三问是带通信约束的联合优化问题，可以使用第二问任一可行运输方案作为搜索初值。完整四问流程默认使用“限时节能”方案作为第三问初值，并在 `q3.json` 的 `search.q2_seed_policy` 中记录。第三问最终运输架次可与第二问主方案不同；两问结果不可混作同一运输计划。`--q3-seed` 可显式选择初值，但更换初值后必须重新求解并核验第三、四问。

## 论文表述边界

题目没有给定唯一的四指标权重，本程序明确给出主目标顺序和五套对照。当前路线搜索按有限候选和确定的搜索预算执行，ALNS 结果也受随机种子、迭代次数和 3% 交付时刻预算影响；这些结果均为经所用物理模型核验的可行方案，不提供全局最优证明。水平航程能耗与爬升附加能耗的具体公式为项目的显式假设，论文须与 `README.md`、`model.py` 的口径一致。
