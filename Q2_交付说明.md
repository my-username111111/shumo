# 第二问运输调度交付说明

## 运行与文件

在本目录执行 `python run.py --only-q2`。程序读取上一级 `数据` 目录的原始附件，把第二问结果写入 `results/q2_only`。完整四问仍执行 `python run.py`。如需指定位置，使用 `--output 目录`。

主要文件：

| 文件 | 内容 |
| --- | --- |
| `q2.json` | 主方案、四套完整备选方案及搜索记录 |
| `q2_transport_sorties.csv`、`q2_box_deliveries.csv` | 主方案的架次和逐箱送达表 |
| `q2_zero_delay_efficient_*.csv`、`q2_delivery_priority_*.csv`、`q2_energy_guarded_*.csv`、`q2_energy_guarded_timely_*.csv` | 四套备选方案 |
| `q2_tradeoff.csv` | 五套方案的五项指标对比 |
| `q2_deadline_audit.csv` | 五套方案逐箱期望时间、硬截止、交付时间与余量 |
| `q2_resource_timeline.csv` | 五套方案每架运输机与电池的任务占用和充电区间 |
| `q2_validation.json` | 货箱、架次、硬时限和备选方案核验数量 |
| `q2_result_template.xlsx` | 原题结果模板的两个 Q2 工作表已填写；其余四问工作表保留空白 |

## 题目约束与目标

每个货箱只能交付一次。每架次可访问一个或多个服务区，货箱不可拆分；航线按原题的节点直线、DEM 净空、载质量、体积和返航余量规则计算。医疗物资的期望送达时间及首批保障截止时间均作为硬约束。运输机和同型共享电池分别占用；电池返航后须按附件的两阶段模型充至 100% 才能再次使用。

主方案按“加权软延误、加权交付时刻、最晚返航、运输能耗、架次数”的字典序比较。加权软延误仅对没有硬截止的货箱计算 `优先系数 × max(0, 实际交付时刻－期望送达时刻)`；加权交付时刻对全部货箱计算 `优先系数 × 实际交付时刻`。硬截止与普通物资期望时间统一放在实际时间轴排序，每个候选计划再逐箱检查硬截止。另保留零软延迟下较省能耗、加权交付时刻优先、限时节能及其时序改良四套完整方案。五套均是搜索得到的可行方案，不声称全局最优。

## 本次复算结果

| 方案 | 加权软延误 / s·权重 | 加权交付 / s·权重 | 最晚返航 / s | 能耗 / kWh | 架次 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 软延误优先，主方案 | 0 | 2,865,941.921 | 7,420.680 | 78.050 | 28 |
| 零延迟较省能耗 | 0 | 2,912,275.622 | 8,791.602 | 77.473 | 27 |
| 交付时刻优先 | 10,639.753 | 2,771,259.248 | 7,629.680 | 73.210 | 25 |
| 限时节能，供第三问接续 | 72,248.943 | 3,203,874.303 | 9,345.191 | 67.929 | 23 |
| 限时节能的时序改良 | 30,223.122 | 3,094,451.202 | 9,377.377 | 67.768 | 23 |

原调度器先安排全部硬截止批次，再安排普通批次，使 10800 秒硬截止货箱排在 3600 秒期望货箱之前。此次修正统一交期排序，并让 C 型机的单服务区大批货箱也可拆分后重新调度。相对修正前主方案，软延误从 94,935.285 降为 0，最晚返航从 10,099.547 秒缩至 7,420.680 秒；代价是能耗从 75.804 增至 78.050 kWh、架次从 24 增至 28。节能方案经架次顺序搜索也把软延误从 489,989.334 降至 30,223.122，同时能耗从 67.861 降至 67.768 kWh。该时序改良版暂未被当前第三问中继搜索覆盖，因此第三问仍从原限时节能版启动。

核验器重算了 80 箱交付、31 箱硬时限、运输航线、载荷、体积、能耗、返航 SOC、电池充电可用时刻和实体资源区间。它还检查无人机、电池编号是否存在及机型是否匹配、架次编号是否唯一、逐箱记录的架次与服务区是否正确、保存的航段阶段是否与重算轨迹一致，以及目标汇总是否吻合。四套备选方案也各自通过同样核验。`tests/test_q2_verifier.py` 的故障注入覆盖虚构编号、错配机型、重复架次、错误交付来源、轨迹篡改和目标篡改，并增加统一交期的回归检查。

## 与第三问衔接

第三问是带通信约束的联合优化问题，可以使用第二问任一可行运输方案作为搜索初值。完整四问流程默认使用“限时节能”方案作为第三问初值，并在 `q3.json` 的 `search.q2_seed_policy` 中记录。第三问最终运输架次可与第二问主方案不同；两问结果不可混作同一运输计划。`--q3-seed` 可显式选择初值，但更换初值后必须重新求解并核验第三、四问。

## 论文表述边界

题目没有给定唯一的四指标权重，本程序明确给出主目标顺序和四套对照。当前路线搜索按有限候选和确定的搜索预算执行，结果为经所用物理模型核验的可行方案，不提供全局最优证明。水平航程能耗与爬升附加能耗的具体公式为项目的显式假设，论文须与 `README.md`、`model.py` 的口径一致。
