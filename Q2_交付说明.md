# 第二问运输调度交付说明

## 运行与交付文件

在仓库根目录执行 `python run.py --only-q2`，程序优先从上一级 `D题/数据`、否则从上一级 `数据` 目录读取原题附件，并把路线修复流程的主方案、八套备选及核验结果写入 `results_q2`。完整四问仍执行 `python run.py`。可用 `--output 目录` 指定输出位置。仓库另有独立的固定路线 CP-SAT 排程实验，结果保存在 `results_q2_refined`；两套结果不能混作同一次求解的备选。

| 文件 | 内容 |
| --- | --- |
| `q2.json` | 主方案、八套完整备选和搜索记录；每架次记录机型、实体机、电池、航线、时刻和能耗 |
| `q2_transport_sorties.csv`、`q2_box_deliveries.csv` | 当前主方案的架次表与逐箱交付表 |
| `q2_quick_24_*.csv`、`q2_energy_22_*.csv`、`q2_prior_soft_delay_priority_*.csv` | 本轮快速、最低能耗和上一版主方案的架次及逐箱表 |
| `q2_<其他方案名>_*.csv` | 原有零延误较省能耗、交付优先、两套限时节能及 ALNS 对照方案 |
| `q2_tradeoff.csv` | 九套方案的加权交付、加权软延误、最晚返航、能耗和架次数 |
| `q2_deadline_audit.csv` | 九套方案逐箱期望时间、硬截止、交付时间和余量 |
| `q2_resource_timeline.csv` | 九套方案的实体机占用、电池占用与充电区间 |
| `q2_validation.json` | 主方案和八套备选的独立回放核验数量 |
| `q2_result_template.xlsx` | 原题结果模板的两个 Q2 工作表，填写当前主方案 |

`soft_delay_priority` 是当前主方案在权衡表中的名称，对应下文的“22 架次交付优先”。`prior_soft_delay_priority` 保存上一版的 28 架次主方案，供逐项对照。`quick_24` 是限时较快方案，`energy_22` 是零软延误条件下本轮最低能耗方案。主方案本身已是 22 架次交付优先方案，因此没有重复保存同名备选。

## 约束、目标与新主方案选择

80 个货箱只能交付一次，不能拆箱。每架次可访问一个或多个服务区，并须满足 DEM 航段净空、载质量、容积、返航安全余量及逐箱交期。医疗物资的期望送达时间和首批保障截止时间作为硬约束；若两者同时存在，取更早者。其余货箱的期望时间用于衡量软延误。实体运输机与同型共享电池分别占用；电池返航后须按附件两阶段充电模型充至 100% 才能再次出发。

主方案仍按“加权软延误、加权交付时刻、最晚返航、运输能耗、架次数”的字典序选择，其中软延误只对无硬截止的货箱计算 `优先系数 × max(0, 实际交付－期望时间)`，加权交付时刻对全部货箱计算 `优先系数 × 实际交付时刻`。硬截止由逐箱核验，不靠罚函数处理。

本轮找到的 22 架次交付优先方案相对上一版主方案，软延误均为零，加权交付时刻从 2,735,557.495 降至 2,730,086.799 s·权重。因此，即使它的最晚返航从 7,558.434 延至 8,027.342 s，按既定字典序仍应成为**本路线修复流程**的主方案。实际决策若更看重最晚返航，可选快速方案；不能把字典序主方案误写为四指标同时最优。

固定路线 CP-SAT 实验在不改变原 28 架次箱组及访问顺序的条件下，进一步优化实体机、电池和起飞时刻，得到零软延误、加权交付 **2,662,962.480 s·权重**、最晚返航 **7,350.407 s**、能耗 **77.059 kWh** 的可行方案。它的加权交付优于本流程的 22 架次主方案，按上述字典序也更优；但多 6 架次、多耗电约 9.454 kWh。该方案存于 `results_q2_refined`，详见 `Q2_精确资源排程实验.md`，且 CP-SAT 状态为 `FEASIBLE`，没有最优性证明。`results_q2` 的主方案选择只在其九套已生成候选中进行，不应称为整个仓库或第二问的全局最优。

## 本轮复算结果

| 方案 | 加权软延误 / s·权重 | 加权交付 / s·权重 | 最晚返航 / s | 能耗 / kWh | 架次 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 22 架次交付优先，当前主方案 | 0 | **2,730,086.799** | 8,027.342 | 67.606 | 22 |
| 24 架次快速方案 `quick_24` | 0 | 2,746,630.503 | **7,531.737** | 71.587 | 24 |
| 22 架次最低能耗 `energy_22` | 0 | 2,811,170.769 | 8,027.342 | **66.961** | **22** |
| 上一版主方案 `prior_soft_delay_priority` | 0 | 2,735,557.495 | 7,558.434 | 77.059 | 28 |
| 零延误较省能耗 `zero_delay_efficient` | 0 | 2,800,522.364 | **7,531.737** | 76.401 | 27 |
| 加权交付优先搜索所得 `delivery_priority` | 10,639.753 | 2,771,259.248 | 7,629.680 | 73.210 | 25 |
| 限时节能 `energy_guarded` | 72,248.943 | 3,203,874.303 | 9,345.191 | 67.929 | 23 |
| 限时节能时序改良 `energy_guarded_timely` | 30,223.122 | 3,094,451.202 | 9,377.377 | 67.768 | 23 |
| 两阶段 ALNS 零延误节能 `alns_zero_delay_efficient` | 0 | 2,809,611.621 | **7,531.737** | 74.337 | 25 |

三套新增方案均完成 80 箱，31 箱硬时限全部满足，无软延误，8 架实体机和 14 组同型电池均参与任务。最小硬截止余量依次为：当前主方案 815.603 s、快速方案 247.682 s、最低能耗方案 833.411 s。快速方案在结果搜索中要求硬截止至少留出 60 s，最终实得余量高于该阈值。`14/14 组电池使用` 表示库存中的每组电池至少参与过一个架次，并不等同于全时段满负荷或证明电池一定是瓶颈；逐组占用与充电见 `q2_resource_timeline.csv`。

与上一版 25 架次 ALNS 方案相比，快速方案在相同的 7,531.737 s 最晚返航和零软延误下，减少 1 架次、节省 2.750 kWh，加权交付时刻也减少 62,981.118 s·权重。当前主方案降至 22 架次、67.606 kWh，并比上一版 28 架次主方案更早完成加权交付，但最晚返航增加 468.908 s。最低能耗方案进一步节省 0.645 kWh，代价是加权交付时刻比当前主方案增加 81,083.969 s·权重。三者是不同偏好下的可行取舍。

## 本轮算法改动

原有 `q2.py` 保留初始组批、路线邻域、实体机和同型电池调度，以及两阶段 ALNS。新增 `q2_refinement.py`，从 ALNS 零延误方案出发，对搬箱、架次合并和多点访问重排进行进一步搜索。

候选排序使用**增量能耗估计**：未变动路线保留当前能耗，只重算受影响路线在各可行机型下的最低能耗。完整调度可能同时改变其他路线的机型，所以这个数只是筛选和排序估计，不是全局能耗下界；真正接受候选前仍用完整调度器重新分配实体机、电池和开始时刻，逐箱检查软延误及硬截止。这样避免按“插入后整条路线的能耗”排序而忽视原路线能耗。每轮按估计值由低到高试算，接受首个满足条件且降低目标能耗的方案，然后重建候选集。搜索至本轮无改进或达到有限轮数。

以旧主方案的加权交付时刻为基准，资源节约搜索要求零软延误，且加权交付不超过基准的 103%，对应上限 2,817,624.220 s·权重。快速分支进一步限定最晚返航不超过上一版 ALNS 方案的 7,531.737 s，并在时序重排中要求每箱硬截止余量至少 60 s。对固定路线集合，分别用 10,000 次和 15,000 次带固定种子的执行顺序扰动，重新计算实体机与电池就绪时刻；保持能耗上限，只接受目标改进。最终再从原主方案和三套新增方案中按既定字典序选择主方案。

本轮独立实验还试用了基于 UCB 的在线算子选择：快速方案与最低能耗方案各评估 1,200 个候选，没有超过上述结果。因此正式求解流程只保留已验证有效的路线修复和时序搜索，没有将未带来增益的算子选择器或尚未训练的 CNN/PPO 写成已实现能力。新增方案的数值可由 `python run.py --only-q2` 从原始附件重新生成。

## 核验、第三问衔接与适用边界

`verify.py` 对主方案及八套备选分别独立回放，重算箱号与目的地、载荷与容积、DEM 航段、时间、能耗、返航 SOC、两阶段充电、逐箱交付和目标汇总；核对实体机与电池编号、机型、占用区间及保存的飞行阶段。`q2_validation.json` 的主方案数量应为 80 箱、22 架次、31 箱硬时限、8 套备选。

第三问增加通信约束，需要重新求解中继和运输联合方案。完整四问流程默认仍从已验证可接续的 `energy_guarded` 方案启动；新的主方案及快速、最低能耗方案都只作为可选运输初值。`--q3-seed` 可显式选择 `quick_24`、`energy_22`、`prior_soft_delay_priority` 或当前主方案的别名 `timely_22`；更换初值后须重新计算并核验第三、四问。

题目未给出四指标唯一权重，本文明确使用字典序并报告多套对照。路线搜索、束搜索、ALNS 和本轮修复均受有限预算限制，所有结果是按本项目明确物理模型核验过的可行方案，不提供第二问的全局最优证明。水平能耗与爬升附加能耗使用 `README.md` 和 `model.py` 中的显式公式假设，论文须如实交代其影响。
