# D 题代码与阶段结果交付说明 v4

编写日期：2026 年 9 月 23 日。

v4 在 v2 可复算方案和 `v3.md` 审查清单的基础上，完成了第一问与公共运输计算核验的专项升级。本文同时承担三项职责：说明已经实现并验证的变化；给出第一问可直接写入论文的结果和最优性边界；保留 v3 对第二至四问的后续优化路线。

版本边界必须明确：本轮没有重写第二至四问的搜索算法。`q2.py`、`q3.py`、`coordination.py`、`q4.py` 和 `run.py` 的求解逻辑保持 v2；公共运输模型和核验器的增强会被这些问题共同使用。v3 中有关候选路线整数规划、连续通信证书和第四问双口径选优的建议仍是后续工作，不能在论文中写成 v4 已实现能力。

## 1 版本结论

| 项目 | v2 / v3 审查状态 | v4 状态 |
| --- | --- | --- |
| 第一问 18 架次 | 在“架次、能耗、时间”字典序下得到 18 架次，缺少简洁的下界闭合证明 | 已用逐服务区重量与体积下界得到 18，再用完整可行方案给出 18，最优间隙为 0 |
| 第一问时间—能耗关系 | 每个货箱子集只保留能耗优先机型，无法形成完整 Pareto 前沿 | 保留每个箱组的全部机型候选，穷尽 18—80 架次并作全局 Pareto 卷积 |
| 返航安全余量 | 只计算 10%、15%、20%、25%、30% 五档，缺少精确切换位置 | 枚举每个箱组/机型的临界 SOC，给出所有架次切换区间及端点证书 |
| 安全载荷异常状态 | 数值 0 同时表示空载不可达和载荷上限为 0 | 新增状态、载荷、空载能耗和限制原因的结构化输出 |
| DEM 穿越 | NoData 像元会被过滤；沿格线航段没有明确的闭单元口径 | 穿过任意 NoData/非有限像元立即报错；格线与角点按闭单元保守覆盖 |
| 输入防错 | Excel 主要依赖固定行号，ID 重复可能被字典静默覆盖 | 增加节点、货箱、机型、实体机和电池类型 ID 唯一性及关键参数检查；表头与单位语义检查仍待补齐 |
| 运输核验 | 与求解器复用主要公式；v3 实验中的虚构机号和电池号能够通过 | 增加独立几何、能耗、时间和载荷回放；检查实体编号、型号、库存电池编号、充电时长和保存的飞行阶段 |
| 故障注入测试 | 已知核验缺口，未形成自动测试 | 增加虚构机号、虚构电池号、重复箱、错区、结果篡改、NoData 和格线边界测试 |
| 复现记录 | 结果目录未入库，缺少第一问独立清单 | `run_q1.py` 输出输入与代码 SHA-256、Python 版本、运行时间和完整机器可读证书 |

在题目给定数据和现有物理口径下，默认 20% 返航余量的第一问结论仍为 18 架次、59.229659919 kWh、累计作业时间 32768.895797 s。升级没有通过改变口径制造新的基线数值；它补足了为什么 18 架次最优、其它时间—能耗选择是什么、余量何时触发换方案，以及这些结果如何被独立算法复核。

## 2 第一问模型与结论适用范围

第一问要求单个服务区往返运输能力和组批方案。每架次从 O01 出发，只访问一个服务区，完成交接后返回 O01；货箱不可拆。此处累计作业时间是各架次准备、装箱、往返飞行和交接时长之和，不是第二问多架实体无人机并行运行时的最晚完工时间。

能耗口径继续沿用：

- 载荷为 q 时的等效航程为 `L(q)=L0-(L0-LF)(q/Q)^1.5`；
- 水平能耗为 `E_use × d/L(q)`；
- 爬升附加能耗为 `(空机质量+当前剩余载荷)×9.81×爬升高度/(爬升效率×3.6×10^6)` kWh；
- 下降不另计附加能耗；
- 每架次满足 `E ≤ (1-ρ)E_use`。

这些公式是项目对题目未完全展开部分采用的明确约定。v4 证明的是这套确定性物理模型下的组合优化最优性，不是对能耗公式的实验标定。爬升效率、水平能耗系数等物理口径扰动仍应作为论文稳健性实验。

## 3 18 架次最优性证明

设服务区 i 的总质量、总体积分别为 W_i、V_i；所有可用机型中的最大载质量和最大容积分别为 Q_max=80 kg、V_max。由于第一问一架次不能跨服务区，服务区 i 至少需要

`N_i ≥ max{ceil(W_i/Q_max), ceil(V_i/V_max)}`。

逐服务区下界可以相加，得到全局下界：

`N ≥ Σ_i N_i = 18`。

| 服务区 | 总质量 kg | 总体积 m³ | 下界 | v4 可行架次 |
| --- | ---: | ---: | ---: | ---: |
| S001 | 154 | 0.394 | 2 | 2 |
| S002 | 81 | 0.211 | 2 | 2 |
| S003 | 81 | 0.211 | 2 | 2 |
| S004 | 59 | 0.156 | 1 | 1 |
| S005 | 59 | 0.156 | 1 | 1 |
| S006 | 59 | 0.156 | 1 | 1 |
| S007 | 45 | 0.129 | 1 | 1 |
| S008 | 45 | 0.129 | 1 | 1 |
| S009—S015 | 每区 25 | 每区 0.067 | 每区 1 | 每区 1 |
| 合计 | — | — | 18 | 18 |

程序同时输出一份逐箱可行证书：80 个货箱恰好出现一次；每架次质量、体积、能耗和返航 SOC 均合规。由“任何方案不少于 18 架次”和“存在 18 架次可行方案”可得第一问最少架次数为 18，整数最优间隙为 0。这个下界证明不依赖动态规划的搜索过程。

默认 18 架次的机型与载荷结构为：

- S001：C 型 78 kg 和 C 型 76 kg；
- S002、S003：每区 C 型 56 kg 和 B 型 25 kg；
- S004—S006：每区 C 型 59 kg；
- S007、S008：每区 C 型 45 kg；
- S009—S015：每区 B 型 25 kg。

完整箱号、体积、单架次能量、水平与爬升分项、质量/体积余量和 SOC 余量见 `results_q1/q1_transport_audit.csv` 与 `results_q1/q1.json`。

## 4 全局时间—能耗权衡

v3 指出，仅修改动态规划的目标顺序不够，因为旧版在进入动态规划前，已经为同一货箱子集删除了时间更优但能耗略高的机型。v4 因此分两层计算：

1. 对每个服务区，按同质量与体积的货箱类型构造计数状态，保留每个非空箱组的全部可行机型；
2. 对每个精确架次数保留全部非支配 `(总能耗,累计作业时间)` 标签，再对 15 个服务区做全局卷积。

没有使用加权和、能耗网格、标签数量上限或按服务区分配固定预算。实际穷尽 18 至 80 个架次后，跨架次数的全局非支配前沿只有两个目标点：

| 方案 | 架次 | 总能耗 kWh | 累计作业时间 s | 时间 h |
| --- | ---: | ---: | ---: | ---: |
| 最省能耗 | 19 | 59.129207797 | 34430.850779 | 9.564125 |
| 最少架次兼最短时间 | 18 | 59.229659919 | 32768.895797 | 9.102471 |

从 19 架次最省能耗方案切换到 18 架次方案，增加 0.100452123 kWh，即 0.169886%，累计作业时间减少 1661.954982 s，即 27.699250 min。两方案的差别集中在 S008：

- 18 架次方案用一架 C 型装载 45 kg，能耗 5.853746998 kWh，耗时 2194.127561 s；
- 19 架次方案把 S008 分成两架 B 型，分别装载 25 kg 与 20 kg，合计能耗 5.753294876 kWh，耗时 3856.082543 s。

其余服务区的全局前沿选择相同。因此，如果论文坚持“架次数优先”，18 架次方案也是固定最少架次数下唯一的非支配目标点，能耗和时间没有内部冲突。如果救援组织允许增加一架次以极小幅度节能，则应并列报告 19 架次方案。

在不限制架次数的总能耗预算模型 `min ΣT, s.t. ΣE≤B` 下：预算低于 59.229659919 kWh 时只能选择 19 架次低能耗点；预算达到该值后，18 架次点更快。相对于全局最低能耗 59.129207797 kWh，切换所需预算增幅为 0.169886%。完整预算情景和逐箱方案保存在 `q1_budget_scenarios.json`。

## 5 返航安全余量临界分析

对一个确定的箱组和机型，航线能耗 E 不随要求的安全余量 ρ 改变，其临界余量为 `ρ*=1-E/E_use`。当 `ρ=ρ*` 时约束取等号，仍然可行；严格超过后该模式失效。v4 枚举全部 742 个箱组/机型模式的临界值，每跨过一个事件便重新优化，不以五档粗网格代替临界点。

| 统一返航余量区间 | 最少架次数 |
| --- | ---: |
| [0%, 22.788615720%] | 18 |
| (22.788615720%, 26.828162524%] | 19 |
| (26.828162524%, 33.422355796%] | 20 |
| (33.422355796%, 33.915271172%] | 21 |
| (33.915271172%, 34.117641448%] | 22 |
| (34.117641448%, 34.164375164%] | 23 |
| (34.164375164%, 34.577002123%] | 24 |
| (34.577002123%, 35.104703262%] | 25 |
| (35.104703262%, 100%] | 全部货箱不可完成 |

首个瓶颈是 S004 的 C 型 59 kg 单架次，其返航 SOC 正好为 22.788615720%；严格提高余量后，该区必须拆分，18 架次不再可行。绝对可行性瓶颈来自 S008，其中 14 kg 饮用水箱的最佳单箱往返由 B 型完成，返航 SOC 为 35.104703262%；严格超过该值后，不论增加多少架次都无法完成全部货箱。

`q1_critical_boundaries.json` 为每个切换点保存等号处可行方案、略高于边界时的最少架次、受影响服务区和临界装载模式；`q1_critical_reserve.json` 保存每个区的全部方案切换以及所有候选事件。论文应使用上述开闭区间，避免把临界点四舍五入后错误归入下一档。

## 6 公共运输计算与核验改动

### 6.1 地形栅格与航段

`model.py` 的 DEM 穿越改为闭单元 supercover：航段经过栅格角点时包含相邻像元，沿格线飞行时同时检查两侧像元。实现按所有横纵格线交点切分参数区间，对区间内部与事件点取样，使用 10^-9 像素格线容差。任一穿越像元为 NoData 或非有限数值时立即报告“未知地形”，不再过滤后使用剩余像元最大值。

独立核验器不用同一遍历代码，而是对候选栅格逐个做线段—闭矩形相交。原始数据中 O01 与 15 个服务区的双向 30 条航段，两种算法得到完全相同的像元集合、最高地形、巡航海拔、爬升、下降和球面距离。测试还覆盖了水平/垂直格线、角点、零长度航段、反向遍历、随机 200 条小航段、越界和部分 NoData。

### 6.2 数据防错

`Scenario` 现在在构造字典前检查节点、货箱、运输机型、实体无人机和电池类型编号非空且唯一，避免重复 ID 被静默覆盖。运输数据还检查：

- 节点坐标、高程有限且位于有效 DEM 内；
- 货箱质量与体积为正、服务区存在；
- 载重、容积、速度、航程、电池能量、爬降速度和效率为正；
- 满载航程不超过空载航程，爬升效率位于 (0,1]，返航余量位于 [0,1)；
- 准备、装卸与交接时间非负；
- 各机型有正数电池库存和充电时间，实体机引用已知机型。

Excel 仍按题目原始附件的固定工作表与行位置读取。v3 提议的表头文本、单位列和完整字段模式验证尚未实现，属于后续公共数据层改进。

### 6.3 独立运输回放

新增 `transport_check.py`，刻意不调用主模型的 `route`、`flight_energy`、`flight_time`、`node_leg` 或 `DEM.peak`。它独立完成：

- 球面距离与地形最高点；
- 每段剩余载荷；
- 载荷相关航程、水平能耗和爬升能耗；
- 准备、逐箱装载、飞行、交接与逐箱送达时刻；
- 返航 SOC、质量和体积；
- 给定安全余量的可行性。

`verify.py` 在原有模型回放之外增加这套独立计算，并检查架次编号、实体机编号及机型、电池编号及型号、非负开始时间、充电完成时刻、保存的载荷体积和 `route.phases` 的阶段类型、起止时间及三维位置。

v3 已证明的故障“把机号、电池号改成 U_FAKE/A_FAKE 仍能通过”已经修复，并成为自动故障注入测试。既有第二问 23 架次、80 箱、31 个硬时限箱的导出结果已用新核验器回放通过。这说明新检查没有否定现有第二问基线，但不构成第二问全局最优性证明。

### 6.4 两套独立优化核对

主前沿算法 `q1_frontier.py` 按同质量/体积货箱的数量建立状态；参考算法 `q1_reference.py` 按 80 个实际箱号的位掩码建立状态。两者使用不同的候选组织和支配扫描方向：

- 主算法穷尽每个精确架次数的非支配标签；
- 参考算法直接穷尽不限制架次数的逐箱 E/T 前沿；
- 临界余量参考算法采用 max–min 递推：对每个精确架次数最大化所有架次中最小返航 SOC，再跨服务区合并。

两套算法得到相同的两个全局 E/T 点和全部返航余量架次切换阈值。小型人工实例另用真正的分区与机型笛卡尔积穷举进行三方对照，防止两个动态规划共享同一种递推错误。

## 7 输出、测试与复现

第一问独立入口：

```powershell
python .\run_q1.py --data-dir "D题\数据" --output .\results_q1
python -m unittest discover -s .\tests -v
```

完整运行不会调用第二至四问。当前验收结果：

- 80 箱、18 架次基线独立回放通过；
- 30 条双向直接航段的两套栅格几何完全一致；
- 742 个装载模式逐一独立复算；
- 18—80 架次共 740 份固定架次数前沿证书逐一复算；
- 121 个全局临界细分区间及所有本地端点通过独立 max–min 算法核对；
- 15 个服务区分别按能耗优先、时间优先做 30 次位掩码/计数状态交叉核对；
- 12 项自动测试通过；
- 既有第二问 23 架次导出结果通过新运输核验。

浮点目标采用 10^-9 kWh 与 10^-6 s 支配容差；两个独立实现的汇总比较允许 10^-8 kWh 与 10^-5 s；几何格线容差为 10^-9 像素。18 架次下界是整数证明；能耗、时间和临界余量是上述数值精度下的穷举结果。

| 文件 | 内容 |
| --- | --- |
| `q1.json` | 原目标顺序下的默认完整方案 |
| `q1_optimality.json` | 每区下界、可行上界、全局 gap=0 证明 |
| `q1_patterns.json` | 同质箱分组和全部箱组/机型模式 |
| `q1_frontiers.json` | 18—80 架次全部局部前沿证书及跨架次全局前沿 |
| `q1_global_frontier.csv` | 两个全局非支配目标点 |
| `q1_budget_scenarios.json` | 最少架次政策和不限架次政策下的全局能耗预算方案 |
| `q1_critical_reserve.json` | 全部候选临界事件、各区方案区间和全局细分区间 |
| `q1_critical_boundaries.json` | 架次数改变的八个边界及等号可行证书 |
| `q1_critical_count.csv` | 合并后的九段最少架次数区间 |
| `q1_transport_audit.csv` | 默认方案逐架次载荷、能耗分项、余量与时间 |
| `q1_safe_payload.csv` | 45 个服务区/机型组合的安全载荷状态 |
| `q1_geometry_audit.csv` | 30 条往返航段的地形与几何复核 |
| `q1_validation.json` | 交叉核对数量、参考前沿和既有 Q2 回放结果 |
| `manifest.json` | 输入附件和代码 SHA-256、运行环境与耗时 |
| `第一问升级报告.md` | 面向论文与验收的精简报告 |

## 8 v3 建议的落实状态

### 8.1 已完成

- 修复虚构运输机、电池编号可通过核验的问题；检查型号匹配和真实编号。
- 独立重算运输路径、剩余载荷、时间、能耗、SOC、充电时间及保存的飞行阶段。
- 对关键物理公式和地形穿越增加可手算、小规模穷举、随机几何和故障注入测试。
- NoData 改为失败，DEM 格线/角点采用保守像元覆盖。
- 第一问保留非支配机型候选，完成时间优先、能耗优先和全局 Pareto 比较。
- 安全余量由五档扫描升级为全部临界事件分析，区分空载不可达和零载荷上限。
- 保存每个余量区间的完整方案、机型、箱组、能耗、时间和不可行原因。
- 增加输入摘要、代码摘要、运行环境、数值精度和结果证书。

### 8.2 部分完成

- 数据校验：已检查 ID、数值范围、库存引用和 DEM 有效性；尚未根据表头文字与单位构造完整模式验证。
- 公共核验：运输部分已有独立实现；中继能耗、端到端通信、连续时间区间及第四问资源核算仍主要沿用原模型。
- 敏感性：返航余量完成精确临界分析；水平能耗系数、爬升效率等物理参数扰动尚未实施。
- 复现：第一问结果包完整；四问统一 manifest、结果模板自动填报和最终论文版本绑定尚未完成。

### 8.3 未实施，继续沿用 v3 路线

- 第二问的候选路线集合、路线组合整数规划、实体机/共享电池时序反馈和 ALNS 扩展。
- 第二、三问统一目标政策，以及加权交付、软延误、完工时间、总能耗和两类架次数的 Pareto 对照。
- 第三问逐栅格/逐事件的连续通信区间证书、位置高度局部细化、缺口分段接力和路线/波次组合搜索。
- 通信端到端余量定义、链路损耗与门限扰动、原方案固定评估和允许重优化的鲁棒性对照。
- 第四问两种资源口径分别选优、资源缺口向量与工作量均衡 Pareto、组内资源重新编号的可执行分配证书。
- 结果提交模板、论文、图件和四问结果的最终一致性检查。

## 9 从第一问继续改到最终方案的实施路线

v4 已完成阶段 A。后续建议维持 v3 的顺序，避免在核验不完整时盲目扩大搜索。

### 阶段 A：第一问与公共运输核验——已完成

验收门槛已经达到：默认值复现；18 架次 gap=0；全局 E/T 前沿完整；临界余量端点有证书；独立物理与优化算法一致；已知虚构资源故障被拒绝。

论文可先定稿第一问的模型、下界证明、18/19 架次权衡和安全余量图表。不要把累计作业时间改写成并行完工时间，也不要把当前能耗公式称为实测规律。

### 阶段 B：第二问候选路线与资源调度

1. 将现有 23 架次方案保留为始终可回退的基线。
2. 预计算“货箱集合＋访问顺序＋机型”路线，保存交付相对时刻、能耗、返航 SOC 和充电时间。
3. 用每箱恰好覆盖一次的路线选择模型建立有限候选池精确基准；有限池最优不能宣称原问题全局最优。
4. 把实体机和共享电池占用放入统一时序调度，资源冲突反馈到路线选择。
5. 在同一目标函数下加入拆分、合并、移箱、交换、访问顺序和机型变化的 ALNS 邻域。
6. 对小规模子实例穷举或整数规划，核对启发式的可行性和差距。

验收必须同时报告加权交付时间、加权软延误、最晚返航、能耗、架次数、运行时间和可行率；禁止只挑一个改善指标。

### 阶段 C：第三问连续通信与联合反馈

1. 把运输轨迹按 DEM 栅格穿越、飞行阶段和服务事件分段，形成直连可用区间。
2. 对中继接入与回传两段计算同一时刻的端到端保守余量，无法证明的区间继续细分。
3. 粗网格筛选后局部细化悬停经纬度和高度，允许不同中继任务覆盖不同缺口区间。
4. 联合安排到达、建链、服务、返航、周转和能源组件充电，不把空间覆盖等同于可调度。
5. 将中继等待、能耗与难保障区间反馈给第二问路线和起飞时间搜索。
6. 分别报告固定方案抗扰能力和扰动后允许重优化的结果。

### 阶段 D：第四问与最终交付

第三问最终关系固定后重新生成不可拆任务块。对两组、三组保持精确穷举，分别按“严格保留原编号”和“同类设备组内重新编号”评价全部分区；输出资源缺口向量、均衡指标及可执行编号安排。最后统一生成提交模板、论文表格、路线图、甘特图、通信证书和四问 manifest。

## 10 论文表述建议

第一问可以使用“在所定义的物理口径和单服务区往返规则下，最少架次数经下界与可行证书证明为 18；对全部可行架次数的精确动态规划得到两个全局时间—能耗非支配点”。

第二、三问目前仍应写“搜索得到的较优可行方案”，不能写“全局最优”。第三问的逐秒检查仍是离散数值核验，不能写成连续通信数学证明。第四问只对固定第三问形成的不可拆单元穷尽所有分区，其精确性不能外推为前三问联合模型全局最优。

任何后续数据、能耗口径、余量、通信参数或求解策略变化，都必须重新运行对应核验并更新 manifest。正文、图表、结果模板和交付说明应引用同一次运行，避免混用 v2、v3 审查数值和 v4 第一问证书。

## 11 文件变更清单

| 文件 | v4 变化 |
| --- | --- |
| `model.py` | DEM 闭单元穿越、NoData 失败、ID 与运输参数检查 |
| `q1.py` | 能耗/时间目标切换、余量输入检查、安全载荷状态和错误箱号检查 |
| `q1_frontier.py` | 新增计数状态精确前沿、全局卷积、预算解、下界证书和临界事件分析 |
| `q1_reference.py` | 新增逐箱位掩码参考前沿和 max–min 返航余量算法 |
| `transport_check.py` | 新增独立运输几何与物理回放器 |
| `verify.py` | 接入独立回放，检查真实资源编号、机型、载荷、充电和飞行阶段 |
| `run_q1.py` | 新增第一问专用运行、证书导出、交叉核验和 manifest |
| `tests/test_q1_upgrade.py` | 新增 12 项几何、物理、穷举、临界、故障注入与篡改检测测试 |
| `README.md` | 增加 v4、第一问严格运行入口和新增文件说明 |
| `results_q1/` | 新增可复现第一问结果、审计表和机器可读证书 |
| `交付说明_v4.md` | 本文；汇总 v3 建议落实状态、第一问结果和后续路线 |

原始题目数据没有写入仓库，第二至四问求解文件没有在本轮改写。v2 与 `v3.md` 继续保留，分别用于追溯既有方案和完整审查依据。
