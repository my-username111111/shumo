# D 题代码与阶段结果交付说明 v2

本文只记录 v2。此前固定运输方案的交付说明保存在独立的 `交付说明.md`，其数值属于 v1；运行当前代码的默认流程会生成 v2。复算 v1 通信方案时使用 `python .\run.py --no-coordination`，并指定单独的输出目录，避免覆盖 v2 结果。

## v2 相对 v1 的变化

| 项目 | v1 | v2 |
| --- | --- | --- |
| 问题三求解 | 固定问题二运输架次后配置中继 | 在原方案上搜索整箱分配、架次拆并、顺序及软时限架次插入通信波次，每个候选重新校验 |
| 最终采用的改动 | 无 | 将 T021 插入 10800 s 通信波次；本轮路线邻域候选未进一步改善目标 |
| 问题三加权交付时间 | 5072737.43 s·权重 | 4900750.74 s·权重，下降 3.39% |
| 问题三最晚返航 | 12445.37 s | 12548.73 s，增加 103.36 s |
| 问题三总能耗 | 72.826 kWh | 72.957 kWh，增加约 0.131 kWh |
| 问题四不可拆单元 | 7 个 | 5 个；两组和三组的最低资源缺口未变 |

新增 `coordination.py`，并修改 `q2.py`、`q3.py`、`run.py` 和 `README.md`。思路参考已发表论文中的邻域搜索、多任务调度及中继配置方法，具体算子与校验按本题的不可拆箱、异构机型、DEM 能耗、共享电池和通信约束重新实现。默认预算为 12 次波次插入试算、2 轮路线搜索且每轮最多 3 次完整中继试算；本次共评估 18 个候选，13 个通过完整问题三求解。问题二作为独立运输答案保持 v1 数值。v2 没有全局最优性证明。

## 一、交付范围

本目录 `uav_coordination` 是基于题目原始数据编写的 Python 求解程序。Git 仓库包含代码和说明，不包含题目原始数据与生成的 `results` 文件夹。要重新计算，请另行取得上一级 `数据` 文件夹；程序会从 `../数据` 读取 5 个无人机运输基础数据 Excel 文件和 30 米 DEM 栅格，并重新生成结果。原始文件未被修改。上一级的 `结果提交模板.xlsx` 尚未由程序自动填入；运行后 `results` 中的 CSV 是填写模板的依据。

核心入口为 `run.py`。`model.py` 实现四问共用的地形、航段、能量、电池与通信计算；`q1.py` 至 `q4.py` 分别求四问；`coordination.py` 在问题三中做通信反馈的运输与中继联合搜索；`verify.py` 独立复算约束；`figures.py` 生成检查用图。更详细的模型口径和借鉴的论文见 `README.md`。

## 二、复现方法

在 `uav_coordination` 文件夹打开终端，使用 Python 3.10 及以上：

```powershell
python -m pip install -r .\requirements.txt
python .\run.py
```

运行后查看 `results/summary.json` 和 `results/validation.json`。程序输出到 `results`，再次运行会覆盖同名结果文件。随机搜索使用固定种子，且不依赖商业求解器。执行 `python .\run.py --no-coordination` 可得到原固定运输方案的通信基线；`--promotion-trials`、`--coordination-rounds`、`--coordination-trials` 控制联合搜索预算。

## 三、当前结果

| 问题 | 当前结果 | 结果文件 |
| --- | --- | --- |
| 问题一 | 15 个服务区、3 种机型的安全载荷和组批；共 18 架次，估计能耗 59.230 kWh | `q1_safe_payload.csv`、`q1_batches.csv`、`q1_reserve_sensitivity.csv`、`q1.json` |
| 问题二 | 80 箱由 23 个运输架次完成，运输任务最晚返航时刻 10307.70 s，估计能耗 67.861 kWh | `q2_transport_sorties.csv`、`q2_box_deliveries.csv`、`q2.json` |
| 问题三 | 23 个运输架次加 8 个中继架次；加权交付时间 4900750.74 s·权重，联合任务最晚返航时刻 12548.73 s，估计总能耗 72.957 kWh | `q3_transport_sorties.csv`、`q3_relay_sorties.csv`、`q3_box_deliveries.csv`、`q3_communication.csv`、`q3.json` |
| 问题四 | 固定问题三任务关系后形成 5 个不可拆单元；已穷举 2 组的 15 种、3 组的 25 种分区 | `q4_partition_resources.csv`、`q4.json` |

相对固定问题二运输方案再补中继的基线，问题三的加权交付时间从 5072737.43 降至 4900750.74 s·权重，下降 3.39%。主要改动是将无硬截止时间的 T021 架次插入 10800 s 波次。最晚返航时刻增加 103.36 s、总能耗增加约 0.131 kWh；这是以时效优先的目标顺序得到的权衡。`q3.json` 的 `search.coordination` 记录了基线、试算候选、失败原因和最终选择。问题二的独立运输结果保持原口径。

独立核验记录：80 箱均恰好交付一次；31 箱硬时限货物满足截止时间；检查了载荷、体积、航段能量、返航 SOC、实体无人机、电池及中继能源组件的占用。问题三另做 31543 个逐秒通信状态采样，未发现断联，采样点上的最小最佳链路余量为 1.880 dB。详见 `validation.json`。

问题四按“严格沿用问题三的实体设备编号”核算，最少新增资源：2 组需增加 1 架 C 型运输机和 1 组 C 型电池；3 组需增加 2 架 A 型、1 架 C 型运输机，以及 1 组 A 型、1 组 C 型电池。这是**固定问题三运输与中继任务关系和时间安排**下的分区结果。提前共享中继波次使不可拆单元由原基线的 7 个变成 5 个，分区选择变少，但最低资源缺口未变。对应分组工作量严重失衡，`q4.json` 还保存了均衡方案与资源折中方案。论文中应展示权衡，不能把最少资源分区直接当成综合最优分区。

## 四、图件和填写模板

`results/q3_route_map.png`：DEM 背景上的运输路线与中继悬停位置。

`results/q3_resource_timeline.png`：运输机、中继机占用和直连/中继通信时间线。

`results/q4_partition_map.png`：2 组与 3 组的最少资源缺口分区。

图件主要用于核对和论文初稿；坐标注记、图例及版式可按论文要求再加工。结果模板中的问题一组批、问题二运输架次与逐箱交付、问题三中继架次与通信保障、问题四资源配置，可以分别从同名 CSV 提取。填表后应按 `validation.json` 再复核箱数、时间单位和设备数量。

## 五、建模假设与结论边界

1. 题目给出载荷相关等效航程，但没有完全指定水平飞行和爬升能耗公式。代码把水平能耗写为 `E_use × d/L(q)`，爬升能耗写为 `(空机质量+载荷)×g×爬升高度/爬升效率`，下降不另计能耗。最终论文要明确列出，并建议检验爬升效率、能耗系数的敏感性。
2. 问题一在这套物理口径和字典序目标下为精确逐区组批。问题二、三使用可行方案搜索，**没有全局最优性证明**。论文措辞应使用“得到一组可行调度方案”及“搜索得到的较优方案”。
3. 问题三经过 2 秒和 1 秒步长复核，但这仍是**数值通信检查**，不是连续时间的数学证明。若论文要写“全程连续通信严格成立”，需增加区间最坏情况或保守证书；最小采样余量约 1.88 dB，也值得做链路参数扰动。
4. 资源初始为满电，使用后充到 100% 才能再执行任务；同型电池共享，中继能源组件共享。运输机下一次任务的准备时间计入下一架次，中继机周转按题目给出的 300 s。若采用不同口径，需统一修改 `model.py`、求解和核验。
5. 道路、水体和村镇资料用于场景解释。题目指定节点间直线飞行，因此代码没有附加沿路飞行或避水约束。

## 六、建议队友接手的工作

1. **论文建模与假设**：以 `README.md`、`model.py` 为准统一符号、单位和目标优先级；重点解释 DEM 净空、载荷相关能耗及充电时序。
2. **结果与图表**：从 `results` 的 CSV 填写 `结果提交模板.xlsx`，核对逐箱交付、设备编号、返航时间及分区资源数；根据论文版式重绘三张图。
3. **稳健性与论证**：对爬升效率、返航余量、链路损耗、接收门限做参数扰动；如需严格连续通信结论，补区间证书；对问题四展示资源缺口和组间均衡的权衡。
4. **最终复核**：任何参数或数据变更后重新运行 `run.py`，先看 `validation.json`，再更新论文数字，避免图表、正文和结果模板使用不同版本。

目前这些是可复现的**阶段结果**，适合队内继续建模和写作；提交前还需要完成模板填报、假设确认和论文表述检查。
