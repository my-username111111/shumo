# 第二问：固定路线的精确资源排程实验与交付

本轮只研究赛题第二问。沿用原方案的货箱组批和服务区访问顺序，重新决定每架次的机型、实体无人机、同型共享电池及开始时刻。所有架次仍从 O01 出发并返回 O01；未加入第三问的通信约束。计算使用题目附件中的 80 个不可拆货箱、机型参数、实体机与电池数量、充电曲线、DEM 航线时间及能耗。

## 结果对比

| 比较对象 | 加权软延误 | 加权交付时刻 | 最晚返航/s | 总能耗/kWh | 架次 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 原主方案 | 0 | 2,735,557.495 | 7,558.434 | 77.059459 | 28 |
| **新主方案** | **0** | **2,662,962.480** | **7,350.407** | **77.059459** | **28** |
| 原 25 架次 ALNS 节能方案 | 0 | 2,809,611.621 | 7,531.737 | 74.336636 | 25 |
| **新 25 架次节能方案** | **0** | **2,808,098.948** | **7,531.737** | **74.096973** | **25** |

相对于各自原方案，新主方案的加权交付时刻提前 72,595.015（约 2.65%），最晚返航提前 208.027 秒（约 2.75%），能耗和架次不变。新节能方案的加权交付时刻提前 1,512.673（约 0.054%），节省 0.239662 kWh（约 0.32%），最晚返航和架次不变。两组均是**相对于各自原方案的严格改进**；新主方案与新节能方案彼此仍构成时效、能耗、架次的权衡，不应直接称其中之一为全局最佳。

这里的“加权交付时刻”是各货箱优先级乘实际交付秒数的总和，单位为“优先级·秒”，不是单箱平均等待时间。第二问要求的及时性由硬截止和普通货箱软延误同时检查；两套新方案的普通货箱加权软延误均为零。

## 对题目约束的处理

1. 每箱仅在一架次中完整交付，全部 80 箱覆盖一次；固定原有箱组及服务区访问次序，不改变交付关系。
2. 对各候选机型重新调用既有航段计算核，检查载重、体积、DEM 净空、飞行能耗和返航 SOC。逐箱交付时刻按该架次的实际航线和各服务区交接过程计算。
3. 医疗物资的期望送达时刻和首批保障货箱的首批截止时刻作为硬约束；其余货箱在本实验进一步要求零软延误。这比仅满足题目的软目标更严格。
4. 同一实体无人机的飞行区间不能重叠；同一同型电池从起飞到返航并充满前不能再分配。电池类型与机型一致，机型数量和电池数量均来自附件。
5. 每架次起点、终点均为 O01；总完成时间取最后一架返回 O01，而不是最后一箱交付时刻。

`verify.py::verify_q2` 对导出的完整方案做独立回放：它用另一个运输物理回放器复算载荷、航时、能耗、返航 SOC、充电时间及逐箱到达，复核箱号、实体机和电池占用、硬截止与汇总指标。验证结果为 **80 箱、31 个硬截止箱、28/25 架次全部通过**。核验证书在 `results_q2_refined/q2_validation.json`；逐箱时限在 `q2_deadline_audit.csv`，所有资源占用区间在 `q2_resource_timeline.csv`。

## 方法与求解边界

`q2_exact_schedule.py` 为每个固定架次枚举物理可行机型，建立 CP-SAT 可选区间。实体机占用为“起飞—返航”，电池占用为“起飞—返航—充满”；两类资源分别施加 `NoOverlap`。模型按各箱相对交付时刻限制硬截止和零软延误，并最小化主方案的加权交付时刻或节能方案的总能耗。求解后按选定资源的先后次序将可消除的等待左移，再用连续时间物理值重新构造和核验输出。

| 实验 | 时间刻度 | 上限及目标 | 求解时间 | 求解状态 |
| --- | --- | --- | ---: | --- |
| 主方案 | 1 秒 | 能耗不高于原值；离散完工上限 7,560 秒；最小化加权交付时刻 | 120 秒 | FEASIBLE |
| 节能方案 | 0.1 秒 | 加权交付不高于原值；离散完工上限 7,532 秒；最小化能耗 | 75 秒 | FEASIBLE |

两个离散上限略高于原方案的连续时间数值，以容纳向上取整的航时和充电时长；**最终连续时间方案都不超过原值**。求解器返回 `FEASIBLE`，说明已找到可行改进，但没有给出这两个固定路线排程问题的最优性证明，更不能宣称第二问全局最优。节能实验的离散能耗下界为 74.077863 kWh、离散可行值为 74.096984 kWh；这些界限仅适用于本轮固定箱组及访问顺序的离散模型。8 个并行求解线程下，重新运行可能得到不同但同样可核验的可行方案；已交付 JSON 保存了本次实际结果。

## 交付文件与复现

- `results_q2_refined/q2.json`：新主方案及原主方案、原节能方案、新节能方案，包含全部架次、逐箱交付与指标。
- `results_q2_refined/q2_result_template.xlsx`：以新主方案填写原题结果模板中的两个 Q2 工作表。
- `results_q2_refined/q2_comparison.csv`、`q2_tradeoff.csv`：原方案与新方案逐指标对比。
- `results_q2_refined/q2_transport_sorties.csv`、`q2_box_deliveries.csv`：新主方案的架次与逐箱记录；新节能方案对应文件名含 `refined_energy`。
- `results_q2_refined/q2_resource_timeline.csv`、`q2_deadline_audit.csv`：四套方案的资源占用及逐箱时限审核。
- `results_q2_refined/q2_main_solver.json`、`q2_energy_solver.json`：时间预算、求解状态、离散目标界及连续时间候选指标。

在仓库目录运行，所需原始附件位于仓库上级的 `D题/数据`：

```powershell
python -m pip install -r requirements.txt -r requirements-q2-exact.txt
python q2_exact_schedule.py --case main --scale 1 --time-limit 120 --workers 8 --makespan-cap 7560 --energy-cap 77.0594591 --output results_q2_exact_main_dominance
python q2_exact_schedule.py --case alns --scale 10 --time-limit 75 --workers 8 --weighted-cap 2809611.621 --makespan-cap 7532 --output results_q2_exact_alns_strict
python build_q2_refined.py
python -m unittest discover -s tests -v
```

本次 25 项测试全部通过。两套结果已经通过第二问运输可行性校验；如用于第三问，需要在新运输时序上**重新**求通信保障，不能沿用旧方案的中继排程或宣称已经满足第三问连续通信要求。
