# 第二问：以最晚返航时间为重点的箱级重组

> 本文记录上一轮 24 架次方案。后续 23/24/25 架次对照和目前推荐的数值见 [Q2_时间优化_续跑_23至25架次.md](Q2_时间优化_续跑_23至25架次.md)。

## 已核验结果

第二问的任务完成时间取**最后一架运输无人机返回 O01 的时刻**。本轮仍使用原题 80 个不可拆货箱、15 个服务区、A/B/C 三种机型、8 架实体机、14 组同型共享电池、DEM 航段、20% 返航安全余量和两阶段充电时间。医疗物资与首批保障的硬截止逐箱约束；普通货箱也保持零软延误。因此下面的时间改进没有靠推迟物资交付换取。

| 完整方案 | 架次 | 最晚返航 / s | 加权交付 / 优先级·s | 能耗 / kWh | 加权软延误 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 原最快 `28_earliest` | 28 | 7350.407 | 2662962.480 | 77.059459 | 0 |
| 27 架次，交付受控后继续压缩 | 27 | 6713.977 | 2642861.171 | 74.116970 | 0 |
| 26 架次，交付受控 | 26 | 6403.494 | 2661127.251 | 72.328058 | 0 |
| 25 架次，同完工时间下优化交付 | 25 | 6205.208 | 2608359.353 | 69.910301 | 0 |
| 24 架次，较低能耗对照 | 24 | 6205.208 | 2659731.911 | **69.004487** | 0 |
| 24 架次，提前尾航线 | 24 | 6139.208 | 2644687.640 | 69.879038 | 0 |
| **24 架次，推荐时间方案** | **24** | **6085.916** | **2566956.613** | **69.794284** | **0** |

推荐方案相对原最快方案，最晚返航提前 **1264.491 秒（17.20%）**，加权交付提前 96005.867 优先级·秒，运输能耗减少 7.265175 kWh，架次减少 4。`verify_q2` 独立复算确认其 80 箱恰好交付一次、31 个硬截止箱全部准时、30 个首批箱准时、16 个医疗箱准时；最小硬截止余量为 **85.567 秒**，最低返航 SOC 为 **23.153%**。上述“加权交付”是各箱优先系数乘实际送达时刻的总和，不是最后一箱的送达时间。

较低能耗对照比推荐方案少耗约 0.790 kWh，但最晚返航晚约 119.293 秒、加权交付多约 92775 优先级·秒；它的最小硬截止余量约 75.96 秒。两套方案反映能源与时间的取舍。本轮以完工时间为重点，优先采用推荐时间方案。

## 为什么时间下降

原 28 架次方案的尾架 `T020` 由 B 型实体机 `U06` 在 5067.275 秒起飞，7350.407 秒才返航，只运输 `S003-WAT-01` 和 `S015-WAT-01`。它的起飞受 `U06` 前一架任务返航制约，同型电池已提前约 101.5 秒可用。单纯增加电池或改变尾架电池编号不能消除这段等待。本轮逐箱调整既有航线，再整体重排机型、实体机、电池与起飞时刻：

1. 把 `T020` 的两箱分别移入原 `T018` 的 S003 停靠和原 `T026` 的 S015 停靠，删除空出的 `T020`，得到 27 条路线。
2. 将原 S009 的 `S009-WAT-01` 与 `S009-FOD-01` 合为一条单区路线，得到 26 条路线。
3. 将原 S004 的两箱 A 型路线并入四箱 C 型路线，得到 25 条路线。
4. 将原 S011 的两箱路线与 `S011-FOD-01` 合为一条三箱路线，得到 24 条路线。
5. 将原 S004 尾部 C 型路线中的 `S004-MED-01` 转入已有的 S005→S004 多点路线，尾架飞行时间缩短 66 秒，路线数仍为 24。
6. 将前序 S006→S007 C 型路线中的 `S006-FOD-01` 转入较早的 S006 单区路线；该新路线选 B 型，使 C 机较早释放，路线数仍为 24。

每一步都重新调用 `Dispatch.evaluate` 检查质量、体积、DEM 航段时间、能耗及返航 SOC。对固定箱组，`q2_exact_schedule.py` 的 CP-SAT 模型重新选择可行机型和具体无人机、电池、起飞时刻，限制同一实体机及同一电池的占用和充电区间重叠，并逐箱约束所有硬截止和普通货箱零延误。最终计划再由独立的 `verify_q2` 连续时间回放。

推荐方案的 24 条固定路线在**整数秒 CP-SAT 完工目标**下返回 `OPTIMAL`，离散目标与下界均为 6088 秒；连续时间回放最晚返航为 6085.916 秒。这个最优状态只适用于当前 24 条箱组和访问顺序及保守取整后的排程模型。路线组批还有其他可能，本结果不声称是第二问全局最优。随后在最晚返航仍不超过离散 6088 秒的条件下最小化加权交付，得到推荐方案；该次求解状态为 `FEASIBLE`，未证明加权交付最优。试把一个尾部双箱路线的两箱都并入现有路线以降至 23 架，当前固定箱组在零软延误条件下被判不可行；这也不构成 23 架次全局不可行证明。

## 文件与复现

- `q2_time_tail_repack.py` 至 `q2_time_sixth_repack.py`：六步箱级调整与精确排程，可单独运行。
- `results_q2_time/tail_repack_24_early/`：第六步 24 架次受控和纯最短完工方案，附求解状态与核验摘要。
- `results_q2_time/research_24/s006_food_to_j002_weighted_plan.json`：推荐方案的完整逐架次、逐箱和资源时序。
- `build_q2_time.py`：独立核验所有已保存的时间方案，分别选择最快、时间受控和交付优先方案；`results_q2_time/selection/` 含选中方案 JSON、对比、机型配比、逐架次、逐箱、资源时间线、时限审计、核验证书及填好的原题 Q2 结果模板。

在仓库目录运行。原题附件位于上一级 `D题/数据`。

```powershell
python -m pip install -r requirements.txt -r requirements-q2-exact.txt
python q2_time_tail_repack.py --seconds 30
python q2_time_second_repack.py --seconds 45
python q2_time_third_repack.py --seconds 45
python q2_time_fourth_repack.py --seconds 45
python q2_time_fifth_repack.py --seconds 45
python q2_time_sixth_repack.py --seconds 45
python q2_frontier_refine.py --input results_q2_time/research_24/s006_food_to_j002_plan.json --output results_q2_time/weighted_refine_recheck --objective weighted --makespan-cap 6088 --energy-cap 77.059459 --seconds 75 --seed 2029
python build_q2_time.py
python -m unittest discover -s tests -q
```

CP-SAT 多线程下相同时间预算可能产生另一份可行排程，已保存的 JSON 是本次实际核验结果。`UNKNOWN` 只表示指定预算内没有判定结果，不能当作无解。第三问增加全过程通信保障；若把本轮运输计划用于第三问，须重新求中继和通信时序。
