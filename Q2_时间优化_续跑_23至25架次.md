# 第二问时间优化续跑：23、24、25 架次的可行方案

## 结论与比较口径

运输任务完成时间取最后一架运输无人机返抵 O01 的时刻。以下所有方案均由同一 DEM、箱级物理模型和 `verify_q2` 复算；80 箱各交付一次，31 个硬截止箱、30 个首批保障箱、16 个医疗箱全部准时，普通货箱加权软延误为零。机型与资源仍是 A/B/C 共 8 架实体机、同型共享电池共 14 组，返航 SOC 不低于 20%。

| 方案 | 架次 | 最晚返航 / s | 加权交付 / 优先级·s | 能耗 / kWh | 最小硬时限余量 / s |
| --- | ---: | ---: | ---: | ---: | ---: |
| 原 28 架次最快基线 | 28 | 7350.407 | 2662962.480 | 77.059459 | 581.741 |
| 上一版 24 架次 | 24 | 6085.916 | 2566956.613 | 69.794284 | 85.567 |
| **本轮 23 架次** | **23** | **6112.317** | **2497000.609** | **69.307237** | **316.628** |
| **本轮 24 架次，均衡推荐** | **24** | **6073.208** | **2500844.903** | **69.154516** | **652.158** |
| **本轮 25 架次，时间最快** | **25** | **6054.352** | **2594765.179** | **70.191988** | **815.603** |

本轮 24 架次方案相对上一版同架次方案，最晚返航提前 **12.707 秒**，加权交付减少 **66111.710 优先级·秒**，能耗减少 **0.639768 kWh**；最小硬时限余量从 85.567 秒提高到 652.158 秒。它在这五项比较指标中严格支配上一版，是默认的均衡推荐。25 架次比本轮 24 架次再早返航 **18.856 秒**，但多一架次、增加约 1.037 kWh，且加权交付较晚；需要把最晚返航作为首要指标时选它。23 架次比本轮 24 架次少一架，交付加权值低约 3844.294 优先级·秒，但最晚返航晚 **39.109 秒**，能耗增加约 0.153 kWh。

三套方案的最低返航 SOC 均为 **23.153%**，高于题目基线的 20%。所有时间和能耗均按保存的连续时间方案计算，并非整数求解器显示值。

## 本轮新增的箱级调整

1. **修复两条交叉 B 型航线。**此前 S010 的食品由 S014 航线顺带运输，S014 的食品由 S010 航线顺带运输。本轮把食品各归本服务区，组成 S010 三箱和 S014 三箱单区航线。两条航线时长从各约 2107.281 秒降到 1554.571 与 1844.539 秒，释放 B 型实体机和电池；在已有 24 架次箱组上，最晚返航从 6085.916 降到 6073.208 秒。
2. **23 架次分支。**把原 S007 早期双箱航线的水箱并入已有 S006→S007 C 型航线，把医疗箱并入 S013→S007 A 型航线，删除原航线；再把 `S001-HYG-01` 在两条首发 C 型航线间重分配，均衡两条 C 型关键链。医疗箱 3600 秒硬时限在完整排程和逐箱核验中均满足。
3. **25 架次分支。**把首发 C 型航线的 `S001-HYG-01` 单独安排为 A 型轻货航线，使该 C 型航线从 6 箱减为 5 箱，时长减少 66 秒。新增航线仍在普通货物期望时间之前交付。最终瓶颈转为一条 S005 A 型航线，最晚返航为 6054.352 秒。

另对本轮 24 架次关键 S004 C 型尾架做了单箱迁移筛查：96 个通过载重、体积、DEM 能耗和单航次时限初筛的接收方式都只能使用 C 型接收机；在其他箱组固定、离散完工上限严格降至 6074 秒时，96 个 CP 排程均不可行。这只排除了该**单箱迁移邻域**，不证明第二问没有更快的其他箱组。

## 求解状态与边界

对各自**固定箱组和访问顺序**，保守整数秒 CP-SAT 排程的最晚返航目标分别得到 23 架次 **6114 秒**、24 架次 **6075 秒**、25 架次 **6056 秒**，状态均为 `OPTIMAL`。连续时间复算对应上表的 6112.317、6073.208、6054.352 秒。25 架次方案在离散完工不超过 6056 秒、能耗不超过 70.192 kWh 的条件下，对固定箱组的加权交付目标也达到 `OPTIMAL`；23、24 架次的加权交付细化为 `FEASIBLE`，没有该目标最优性证明。三套完整方案均再次通过 `verify_q2`。

上述证明范围仅是固定箱组及当前物理模型；不能写成第二问全局最优。题目没有完整给出水平飞行与爬升能耗的拆分公式，代码沿用仓库 README 已披露的显式假设。第三问有全过程通信约束；若把其中一套运输计划用于第三问，必须重求中继与通信时序，不能直接沿用旧的第三问结果。

## 交付文件与复现

- `q2_time_zone_fod_repair.py`：S010/S014 食品重新归区、物理筛查与固定路线排程。
- `q2_time_frontier_extension.py`：从修复后的 24 架次箱组构造 23 和 25 架次分支；`q2_time_s004_tail_probe.py` 保存单箱迁移的受限排查过程。
- `results_q2_time/frontier_23`、`frontier_24`、`frontier_25`：三套完整方案及求解报告。
- `results_q2_time/selection/`：`fastest.json` 为 25 架次，`balanced.json` 为 24 架次，`delivery.json` 为 23 架次；同名逐架次、逐箱、资源时间线、时限审计 CSV，`comparison.csv`、`fleet_mix.csv`、`verification.json` 提供复核数据。`q2_time_result_template.xlsx` 已填入**最快的 25 架次方案**的原题两个 Q2 工作表，其余四问工作表保留原模板。

在仓库目录且上一级 `D题/数据` 附件存在时执行：

```powershell
python -m pip install -r requirements.txt -r requirements-q2-exact.txt
python q2_time_zone_fod_repair.py --seconds 45
python q2_time_frontier_extension.py --variant 23 --output results_q2_validation/frontier_23
python q2_time_frontier_extension.py --variant 25 --output results_q2_validation/frontier_25
python build_q2_time.py
python -m unittest discover -s tests -q
```

后两条分支命令重算固定路线的最晚返航；已保存的加权交付细化方案在 `frontier_23`、`frontier_24`、`frontier_25`。CP-SAT 多线程搜索在相同秒数预算内可能得到不同排程；比较和论文数值以已保存、已复核的 JSON 为准。
