# 第二问继续优化：路线合并、精确排程与多目标对比

后续箱级重新组批已找到 20 与 19 架次可核验方案，见 [Q2_继续探索_v2_19架次与路线池.md](Q2_继续探索_v2_19架次与路线池.md)。下文记录本轮当时的 21–24 架次阶段；`results_q2_extended` 现已扩充至 12 套方案。

本轮在原题的 80 个不可拆货箱、DEM 航段、A/B/C 三种运输机及实体机和同型共享电池数量下，继续搜索第二问运输方案。所有架次仍从 O01 起飞并返航，医疗物资和首批保障箱满足硬时限，其余箱进一步要求零软延误；没有加入第三问的通信约束。`verify_q2` 已独立复算每份交付方案的货箱覆盖、载荷、体积、航时、能耗、返航 SOC、逐箱到达和机/电池占用。

## 可直接选用的方案

| 方案 | 架次 | A/B/C 架次 | 加权软延误 | 加权交付时刻（优先级·秒） | 最晚返航 | 能耗 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 原时效主方案 | 28 | 14/8/6 | 0 | 2,662,962.480 | 7,350.407 s | 77.059459 kWh |
| 原 25 架次节能方案 | 25 | 10/8/7 | 0 | 2,808,098.948 | 7,531.737 s | 74.096973 kWh |
| **新 24 架次快速节能方案** | **24** | **9/8/7** | **0** | **2,774,907.904** | **7,531.737 s** | **71.888174 kWh** |
| 新 23 架次均衡方案 | 23 | 8/8/7 | 0 | 2,666,792.761 | 7,663.737 s | 69.946565 kWh |
| 新 22 架次低架次方案 | 22 | 7/8/7 | 0 | 3,398,189.024 | 8,215.878 s | 67.417268 kWh |
| 新 21 架次低架次方案 | 21 | 6/8/7 | 0 | 2,948,652.297 | 8,225.342 s | 66.890893 kWh |

**24 架次快速节能方案严格支配原 25 架次节能方案**：加权交付时刻减少 33,191.044 优先级·秒，能耗减少 2.208800 kWh，架次少 1，最晚返航不变，软延误仍为零。与 28 架次时效主方案相比，它仍更晚交付，不能取代以加权交付时刻为首要目标的主方案。

23 架次均衡方案的加权交付时刻只比 28 架次主方案多 3,830.281 优先级·秒（约 0.14%），能耗低 7.112894 kWh（约 9.23%），少 5 架次；代价是最晚返航晚 313.330 秒。若论文希望展示多目标权衡，这是值得重点呈现的另一端。21/22 架次方案进一步节省能耗和架次，但不应仅凭架次少就称为“最好”。

`results_q2_extended/comparison.csv` 给出各套方案的硬时限余量、逐箱时效、A/B/C 架次和实体机启用数等完整对照；`fleet_mix.csv` 将 **A/B/C 架次** 与 **实际使用的实体机架数** 分开统计。原 25 架次节能方案在五指标比较中被 24 架次方案支配，其他列出的方案存在不同取舍。此处“加权交付时刻”不是最大完工时间，二者分别统计。

两份优先推荐方案都使用题目给定的 4 架 A、2 架 B、2 架 C 实体机，31/31 个硬截止箱准时；24/23 架次方案的最小硬时限余量分别为 454.06/485.82 秒，最低返航 SOC 都为 22.79%，高于 20% 的要求。

## 新方法与求解边界

1. `q2_merge_search.py` 从一份完整可行解出发，枚举两架次合并及两种服务区先后顺序。合并后重新评估 A/B/C 机型的载重、体积、DEM 航段、返航 SOC 与逐箱相对交付时刻；仅把通过物理和单航次时限初筛的箱组送入资源排程。
2. `q2_exact_schedule.py` 用 CP-SAT 为固定箱组同时选择机型、实体机、同型电池和起飞时刻。飞机占用至返航，电池占用至返航后按附件曲线充满。目标可取能耗、最晚返航或加权交付时刻，并可加显式能耗与完工上限；导出后再按连续时间物理模型独立复核。
3. 从 25 架次解逐步得到 24、23、22、21 架次方案，然后在 24 和 23 架次固定箱组上重新优化加权交付时刻。求解器对关键加权交付实验返回 `FEASIBLE`，**没有**证明固定箱组最优，更没有证明第二问全局最优。
4. 在保存的 18 种不同 21 架次箱组中，`q2_audit_20_branches.py` 测试了 857 个通过单次合并初筛的 20 架次候选，均未找到资源与零软延误同时可行的排程。这个结论只限于这批箱组及单次合并邻域；仍可能有其他 20 架次甚至更少的全新组批。外部截图只有汇总数字，缺逐箱记录、航线和资源时间轴，因此不能直接证明它与本仓库使用相同口径。

## 交付和复现

- `results_q2_extended/plans/`：8 套逐架次、逐箱完整 JSON。
- `results_q2_extended/verification.json`：每套方案通过 80 箱、31 个硬截止箱的独立核验记录。
- `results_q2_extended/result_template_24_fast_efficient.xlsx` 与 `result_template_23_balanced.xlsx`：分别填写 24/23 条运输架次和 80 条逐箱交付，供队友选择后填入总提交模板。
- 同目录的 `sorties_*.csv`、`deliveries_*.csv`、`comparison.csv`、`fleet_mix.csv`：论文表格和复核用。
- `results_q2_extended/search_20_summary.json`：20 架次局部搜索的准确范围与逐分支结果。

仓库目录下使用已有 `.venv` 或先安装 `requirements.txt` 与 `requirements-q2-exact.txt`。复算现有交付：

```powershell
python build_q2_extended.py
python -m unittest discover -s tests -v
```

继续搜索：

```powershell
python q2_merge_search.py --input results_q2_refined/q2_energy_plan.json --limit 30 --seconds-per-candidate 5 --makespan-cap 7540 --output results_q2_merge_search_cap7540
python q2_frontier_refine.py --input results_q2_merge_search_cap7540/best_plan.json --output results_q2_frontier/new_trial --objective weighted --energy-cap 72 --makespan-cap 7540 --seconds 60
```

上述搜索含多线程和随机性，重新运行可能获得不同但仍可核验的方案；交付目录保存的是本次实际算出的完整解。此次改动只改善第二问运输结果。如果把 23/24 架次方案用于第三问，需要重新求连续通信保障，不能沿用旧运输轨迹的中继排程。
